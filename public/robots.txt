User-agent: *
Allow: /

# Скачиваемые прайсы закрыты от обхода (23.09.2026). Не от людей — robots.txt на них
# не влияет, файл по-прежнему скачивается по ссылке с главной.
#
# Причина: имя файла содержит дату («2026-09-23-zilma-price.xlsx»), то есть каждый день
# появляется НОВЫЙ адрес. Яндекс их исправно обходил — и в журнале обхода 8 записей из
# 10 последних оказались файлами прайса. При обходе всего 14 страниц в месяц это значит,
# что робот тратил его почти целиком на таблицы, а не на статьи и каталог.
#
# Накапливать вес такие адреса всё равно не могут: завтра у файла другое имя.
Disallow: /prices/

Sitemap: https://www.zilma.pro/sitemap-index.xml
