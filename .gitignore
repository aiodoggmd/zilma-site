# build output
dist/
.vercel/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# Windows folder-view metadata
desktop.ini

# jetbrains setting folder
.idea/

# local Claude Code settings (machine-specific, no secrets but not for repo)
.claude/settings.local.json

# staging/debug images from article-building workflow (kept on disk, not for repo)
_tmp_*
/2026-08-16-koleston-perfect-shade-chart/

# loose/stray files swept up from the repo root (old drafts, one-off experiments) —
# kept on disk in case they're needed later, never meant for the repo
/_trash/

# Playwright MCP test artifacts (screenshots/logs from browser-automation testing sessions)
.playwright-mcp/

# accidental clipboard-paste filename artifact
Хочу создать сайт*

# raw price files — already served from public/prices/, kept here as originals only.
# Скрипт конвейера — не данные, его нужно версионировать (без этого правки вроде
# фикса 16.09.2026 в header_level() жили бы только на диске). Игнорировать надо
# СОДЕРЖИМОЕ (/Price/*), не саму папку — иначе git не спускается внутрь неё и
# файловое исключение ниже молча не работает (проверено).
/Price/*
!/Price/build_price_current.py

# reference screenshots used during design (not site content)
/тут.jpg
/цвета.jpg
/структура сайта.jpg
/ориентир цветов.jpg
/блок1.png
/блок2.png
/Снимок экрана*.jpg

# локальное python-окружение (psd-tools для разбора архива фото)
.venv/

# Боевые настройки приёмника заявок: токен бота и пароль на просмотр
deploy/config.php
deploy/orders-data/

# Доступы к хостингу для выкладки
.env.deploy

# MCP-конфиг: содержит токен телеграм-бота и ключи API
.mcp.json
__pycache__/
