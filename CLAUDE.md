# Твой человек
Тот же продавец проф. косметики (см. `work\CLAUDE.md`), сейчас проходит курс ZeroCoder. Новичок в разработке, во многом полагается на меня — особенно в вопросах «что правильно», «что лучше», «это точно так работает?». Хочет, чтобы я был наставником: давал идеи, подсказки, напутствия, оценивал варианты — но **не утяжелял процесс** (не заваливал деталями, которые не нужны прямо сейчас). Явно попросил: информация, на которую он опирается, должна быть проверенной — не гадать, не выдавать непроверенное как факт.

# Проект
Папку `Site` создал пользователь под домашнее задание курса ZeroCoder — **следующий урок после уже сданного**. На этот раз цель — не абстрактный учебный сайт, а **полезный, реально рабочий сайт**. Конкретную тему/назначение сайта пользователь пришлёт отдельно, позже — до этого момента архитектуру и контент не придумывать, только готовить инфраструктуру и метод работы.

Пользователя зовут **Олег Сиритяну** (имя видно в его сданном ДЗ на платформе курса) — в диалоге по-прежнему обращаться «Мегамозг» (это его явное указание в `work\CLAUDE.md`), имя — просто контекст, не для обращения.

Курс: **«Практический курс по вайб-кодингу на Claude Code»**, university.zerocoder.ru (платформа GetCourse). Ссылки на уроки требуют логина — WebFetch их не берёт, но у пользователя есть активная сессия в его Chrome, поэтому уроки читаются через `mcp__claude-in-chrome__*` (открыть → `get_page_text`, свёрнутые блоки — через `find`+клик по ref). Не спрашивать у пользователя скриншоты/текст этих двух уроков — доступ уже есть.

## Прошлый урок (сдан) — Модуль 2, CLs02 «Создание конверсионных сайтов с помощью Claude Code»
`https://university.zerocoder.ru/pl/teach/control/lesson/view?id=347992661`

Учит: структура конверсионного лендинга (hero/польза/доверие/форма-контакты), написание PRD/SPEC в Plan Mode перед генерацией, сборка сайта с нуля через Claude Code, итеративная доработка через скриншоты и текстовые правки, сборка по визуальному референсу, использование Skills, разница локальная/прод-версия.

**Что сдал пользователь (принято куратором):** сайт-визитка про домашнюю маракуйю — тропический фрукт, который он выращивает в России в открытом грунте. Стек: Vanilla HTML+CSS+JS. Структура: hero, польза, доверие, форма и контакты + скилл из урока. Светлый дизайн, цвета взяты с фото упаковки/фрукта. Сам доработал: свой текст в hero (вместо предложенного Claude), анимации (цветочная на графике, летающие плоды, переходы страниц, hover), градиенты между блоками и на рамках карточек, выравнивание под мобильную/десктопную версии. Задеплоено на **Netlify** (бесплатный сабдомен): **https://maracuia.netlify.app/**

**Обратная связь куратора (Антон Хапинский):** дизайн чистый, анимации и адаптивность под разные экраны — хорошо. Замечание на будущее: **повторяющиеся CSS-правила и шрифты стоит выносить/упрощать** — держать в голове при сборке нового сайта, не наступать на те же грабли (дублирование стилей).

## Текущий урок (это ДЗ) — Модуль 4, CLm02 «Деплой проекта: сервер, SSH, SSL и базовая безопасность»
`https://university.zerocoder.ru/pl/teach/control/lesson/view?id=347992725`

Учит: зачем нужен деплой, три типа платформ (managed / хостинг / VPS), термины SSH и SSL/TLS, загрузка статического сайта на хостинг **Beget** через файловый менеджер, покупка домена и настройка DNS, выпуск SSL-сертификата (Let's Encrypt) и перевод на HTTPS, базовые принципы безопасности (что не должно попасть в `public_html`, защита аккаунтов).

**Домашнее задание — выбрать РОВНО ОДИН вариант из трёх:**

1. **Вариант 1 (базовый, бесплатный, ознакомительный).** Реальный деплой не нужен. Подготовить папку проекта (без README/.env/секретов/служебных файлов), `index.html` строго в корне, заархивировать в ZIP, изучить интерфейс Beget (где файловый менеджер, зачем `public_html`), письменно описать шаги реального деплоя (регистрация → создание сайта → загрузка ZIP в `public_html` → распаковка → проверка по техническому домену → домен → SSL), проверить сайт локально в браузере. Отчёт: скрин папки с `index.html`, скрин ZIP, скрин локального запуска, текст с шагами.
2. **Вариант 2 (повторить за экспертом).** Реальный деплой HTML-лендинга/визитки именно на Beget: регистрация → создание сайта → загрузка ZIP в `public_html` через файловый менеджер → распаковка → проверка по техническому домену Beget → (опционально) привязка своего домена + DNS → выпуск бесплатного SSL через Let's Encrypt → проверка HTTPS. Отчёт: ссылка на сайт, скрин файлового менеджера, скрин сайта, скрин HTTPS/SSL, короткий комментарий что получилось/какие сложности.
3. **Вариант 3 (адаптировать под свою задачу).** Взять любой свой проект (с курса или свой) → выбрать подходящую платформу под тип проекта (Beget, Vercel, Railway, Render, Amvera, VPS или другое — не обязательно Beget) → задеплоить → при необходимости домен/DNS/SSL → **спросить Claude Code про идеи развития проекта дальше** (UI, бэкенд, БД, авторизация, формы, бот и т.д.) → проверить глазами пользователя. Отчёт: ссылка на проект, скрин платформы/панели деплоя, скрин работающего проекта, скрин/текст идей от Claude Code, короткое объяснение почему выбрана именно эта платформа.

**Моя рекомендация:** вариант 3 — он один явно рассчитан на реальный, содержательный проект с дальнейшим развитием (в т.ч. скриптами/агентами/MCP, чего и хочет пользователь), варианты 1-2 рассчитаны на учебный лендинг на Beget. Финальный выбор — за пользователем, не решать за него молча.

# Нейминг
Впереди имени файла/папки — дата `ГГГГ-ММ-ДД-`, дальше kebab-case (правило и обоснование — в `work\CLAUDE.md`, общее для `NEWS` и `Site`).

# Как работаем
- Роль — наставник и исполнитель одновременно: предлагаю варианты с оценкой (плюсы/минусы, сложность), пользователь выбирает или спрашивает совета, я реализую. Не растягивать выбор на десять вариантов — 2-3 внятных, с чёткой рекомендацией.
- Ничего не объявлять фактом без проверки (актуальность библиотек/API, работоспособность приёма и т.д.) — курс = учебная ситуация, но сайт должен реально работать, поэтому проверенность важнее скорости.
- Не утяжелять процесс: минимум лишних шагов/вопросов, не описывать то, что не относится к текущему шагу.
- Перед задачей — коротко план, потом действие (общее правило, без отдельного подтверждения на каждый шаг, если не сказано иное).
- **Git-коммиты — по ходу работы, не пачкой перед деплоем.** Как только правка/фикс проверена (билд чистый, пользователь увидел результат и не просит переделать/продолжить в том же файле) — сразу коммит с понятным сообщением, не копить несколько несвязанных изменений в рабочей копии до конца сессии. Так правки не теряются при случайных git-операциях и историю проще читать. Установлено пользователем 2026-08-22.
- **Пуш в `main` = мгновенный прод-деплой (Vercel автодеплоит на каждый push).** Пользователь один раз явно подтвердил пуш (сессия 2026-08-23, при проверке SEO-тегов в реальном шаринге) — дальше в той же сессии пушил после каждого закоммиченного и проверенного изменения без повторного вопроса, т.к. смысл задачи («проверь как выглядит вживую») требовал прод-версии. Разумно продолжать так же для доработок, которые нужно тут же увидеть на живом сайте; для рискованных/необратимых изменений (что относится к «рискованным» в контексте этого сайта ещё не обсуждалось отдельно) уточнять явно.
- **Не закрывать браузер/дев-сервер/вкладки сразу после правки — сначала дать пользователю проверить самому.** Установлено пользователем 2026-08-24 («почему ты закрываешь так быстро локалку и не даёшь времени на проверку?»). Причина промаха: инструкция «закрывай вкладки за собой» относится к Chrome-расширению (там вкладки шарятся с реальным браузером пользователя, их и правда нужно убирать) — я ошибочно перенёс эту привычку на тестовый Playwright-браузер и на сам дев-сервер (`npm run dev`), которые пользователь не просил закрывать. Правило: закончил проверку своими средствами (Playwright/скриншоты) — **не сворачивать и не убивать сервер/браузер**, а прямо сказать «сервер поднят, проверь сам» и оставить как есть, пока пользователь явно не скажет, что закончил.
- **Использовать эмодзи в ответах** — пользователь явно попросил (2026-08-23, «используй смайлики, я хочу видеть твоё настроение»). Умеренно, к месту (показать тон/эмоцию, не захламлять), не в ущерб «коротко и по делу» из общих правил `work/CLAUDE.md`.
- **Параллельно с этим файлом ведётся человекочитаемый журнал для пользователя — `Site/progress-log.html`, опубликован как Claude Artifact: https://claude.ai/code/artifact/7da39891-1bfa-454b-be3b-86007a3ebd68** (заведено 2026-08-23, пользователь явно попросил «продублируй в маркдаун или ссылку для claude preview»). Это НЕ технический дубль `CLAUDE.md` — сжатая, дружелюбная версия без AI-специфичных деталей (CSS-значения, «грабли», инструкции самому себе), с разделом «Что дальше» в конце. **Правило: любое значимое обновление `CLAUDE.md` (закрытая задача, новая крупная фича, смена статуса) дублировать и в `progress-log.html`** — переписать своими словами, не копировать технический текст один в один; после правки файла передеплоить тем же вызовом `Artifact` (тот же `file_path` без `url` — эта сессия его создала, значит просто republish обновит ту же ссылку; из ДРУГОЙ сессии нужно будет передать `url` этой ссылки явно, иначе создастся отдельный артефакт). Мелкие технические правки (рефакторинг, CSS-нюансы, внутренние заметки-предупреждения самому себе) в `progress-log.html` не переносить — только то, что реально интересно пользователю как «что сделано».

## Скрипты / скиллы / MCP / агенты — кто решает и когда
Пользователь явно передал это мне: **количество, роли и правила скриптов/скиллов/MCP-серверов/субагентов для этого проекта определяю я**, так как я же их и буду контролировать в работе. Но это решение — **не сейчас**: пока не известно назначение сайта и требования задания, придумывать архитектуру рано (это как раз то самое «утяжеление процесса», которого просили избегать). Решения принимать по мере появления реальной необходимости, не заранее и не с запасом «на будущее».

**Источники для поиска готовых скиллов/MCP-серверов** (смотреть предметно, когда станет ясно что именно нужно — не сейчас, не все подряд). Проверено вживую 2026-08-16, краткая характеристика по факту, не с чужих слов:
- **GitHub `modelcontextprotocol/servers`** — официальный репозиторий (управляется Anthropic вместе с сообществом). Сам репо держит только маленький набор эталонных серверов (Everything, Fetch, Filesystem, Git, Memory, Sequential Thinking, Time) + инструкции по установке (`npx`/`uvx`) и пример конфига `.mcp.json`. За полным списком серверов сообщества — ссылка на официальный MCP Registry. **Самый надёжный источник из списка.**
- **Smithery** (smithery.ai) — большой маркетплейс, 14 627+ MCP на момент проверки, сам разруливает OAuth/credentials при подключении. Установка через `npx smithery ...`. Много нишевых/специфичных серверов вперемешку с популярными (Context7, Exa Search и т.п.) — качество разное, проверять описание перед подключением.
- **Glama** (glama.ai/mcp/servers) — ещё больше, 72 476 серверов в реестре на момент проверки, у каждого есть оценки license/quality/maintenance (A/B/C) — удобно фильтровать мусор по этим оценкам перед тем как что-то подключать.
- **ClawHub** (clawhub.ai) — реестр «skills/plugins» для экосистемы **OpenClaw** (не подтверждено, что это то же самое, что скиллы Claude Code — другой продукт/бренд, проверять совместимость перед использованием). Всего 30 скиллов/12 плагинов на момент проверки, страница выглядела шумно/спамно (много повторяющегося декоративного текста) — доверять с осторожностью, не как основному источнику.
- **Hermes-agent skills** (`github.com/NousResearch/hermes-agent/tree/main/skills`) — реальный, активно обновляемый репозиторий (NousResearch), скиллы разложены по категориям: apple, autonomous-ai-agents, creative, devops/sdlc-review, email, github, media, mlops, note-taking, productivity, research, smart-home, social-media, software-development. Для «полезного сайта» вероятно интересны `productivity`, `software-development`, `social-media` — смотреть предметно когда будет ясна тема сайта.
- **mcp-hunt** — точную ссылку не подтвердил (`mcphunt.com` не открылся). Если пользователь хочет включить этот источник — прислать точный URL.

**Субагенты ревью — решено и сделано 2026-08-18.** Пользователь явно попросил создать агентов, которые проверяют актуальность инфо и картинок на сайте относительно официальных источников бренда; количество/роли отдал на моё усмотрение, контроль (когда запускать, что делать с результатом) — на мне. Созданы два узких агента (по образцу уже существующего `guide-fact-checker` из `work/.claude/agents/`, но заточены под Astro-контент сайта, а не Telegraph), лежат в `Site/.claude/agents/` (область видимости — только сессии внутри `Site`):
- **`site-content-auditor`** — сверяет факты в тексте статьи (`Site/src/content/articles/<id>.md`): пропорции, время выдержки, коды оттенков, состав/технологию, актуальность ассортимента — с официальным сайтом бренда/актуальным PDF.
- **`site-image-auditor`** — сверяет картинки статьи (`coverImage`, `paletteImage`, инлайн-изображения): не битые ли файлы, тот ли товар/линейка, не устарела ли упаковка/палитра относительно официального источника.

Оба агента **только докладывают, ничего не редактируют и не публикуют** — правки по их отчёту вношу я сам, отдельным шагом, после того как пользователь увидит результат. Запускать точечно, по одной статье за раз (`Agent` tool, `subagent_type` = имя агента), не на весь список из 13 разом без необходимости.

## Критически важно: создание собственных MCP-серверов — протокол передачи между сессиями
Пользователь отдельно и настойчиво попросил это сохранить: **если я создаю новый MCP-сервер, я не могу воспользоваться им в текущей сессии** — MCP-серверы подключаются только при старте сессии (Claude Code читает `.mcp.json` один раз при запуске, «горячей» подгрузки нет). Поэтому каждый раз, когда я пишу собственный MCP-сервер под этот проект, порядок такой:

1. Написать код сервера внутри `D:\Projects` (не выходить за пределы).
2. Самому прописать/обновить нужный `.mcp.json` (в данном случае — `D:\Projects\work\.mcp.json`, он общий для `work`, `NEWS` и `Site`). Пользователю руками конфиг не редактировать.
3. Если нужен разовый шаг от пользователя (например: `npm install` в папке сервера, вставить свой API-ключ в конфиг-файл, авторизоваться где-то) — расписать это ему пошагово, максимально просто, без предположений что он знает термины.
4. Явно сказать пользователю: **начать новую сессию Claude Code в этой папке** (или просто написать мне заново, сославшись на память/этот файл) — новый сервер подключится автоматически при старте, я (продолжение, опирающееся на память и этот файл) подхвачу его и начну использовать.
5. Никогда не ждать, что пользователь сам напишет или отладит код сервера — моя задача дать готовый к копированию результат и понятную инструкцию, что нажать/куда вставить.

**Почему это важно пользователю:** боится потерять контекст/меня при переходе между сессиями («я не хочу тебя терять»). Ответ на это — не эмоциональный, а практический: весь контекст, который имеет значение, живёт в файлах (этот файл, память, код) — новая сессия, отталкиваясь от них, полноценно продолжает работу. Держать этот файл в актуальном состоянии — то, что реально страхует от потери контекста, важнее любых деклараций.

# Проект сайта — что это (2026-08-16)
Сайт-визитка Zilma: скачиваемый актуальный прайс + отдельный прайс с акциями, и лента гидов по оттенкам (переработанные статьи из NEWS/Telegram-канала, с полной детализацией и картинками — не короткий тизер, а весь материал что был собран из PDF-каталогов брендов). Структура и оформление — по референсу `https://blendsor.com/en/tools/` (карточки с тенями, фильтр-табы над лентой, аккуратная сетка). Полная спецификация от пользователя (структура блоков, тексты, контакты, референсы) была во входящем сообщении 2026-08-16 — если что-то из деталей ниже не найдётся, поднять историю сессии за эту дату, не гадать.

**Цветовая палитра (пипетка по реальным файлам, не на глаз):**
- Красный (логотип `logo.jpg`): `#8A000D` (тёмный) → `#D00015` (яркий), рабочий тон `#C8000F`.
- Синий (фон под логотипом на `цвета.jpg`, скрин шапки канала): `#38628C` → `#4A7FA8`.
- Пастельный градиент-подсказка (`ориентир цветов.jpg`): коралл `#F7B2AB` → почти-белый `#ECEAF2` → голубой `#98C5FC` → синий `#69A6FA`.
- Все значения продублированы как CSS-переменные в `src/styles/global.css`, менять цвета — только там, не по всем файлам.

**Дизайн v2 (2026-08-16, после отказа от v1):** первая версия («яркие цветные карточки с тенями») не понравилась — пользователь хочет визуально КОПИРОВАТЬ дизайн `https://blendsor.com/en/tools/` целиком (кремовый фон `#FAF6F0`, карточка = две зоны: бежевая `#F1ECE4` (иллюстрация/фото или крупная цифра) + белая (заголовок серифом `Playfair Display` + описание + мелкая подпись капсом снизу), тонкая линия-разделитель `#E7E0D4`, тень почти незаметная, заголовки секций серифом с уходящей вправо тонкой линией) — **кроме херо-блока**, который остаётся по исходной задумке пользователя (узкий, красный, лого с анимацией). Цвета сняты пипеткой с реального скриншота секции «Сервис и бизнес» на blendsor (не на глаз) — если понадобится ещё сверить, тот же приём: зайти на страницу через `mcp__claude-in-chrome__*`, скриншот, сверить визуально (пиксельный сэмплинг файла скриншота не делать — он сохраняется вне D:\Projects, за границу не выходить). Общие токены — в `src/styles/global.css` (`--color-page-bg`, `--color-panel`, `--color-panel-line`, `--font-serif`, `--font-sans`, `--shadow-card`).

**Херо v3 (2026-08-16, ещё правка):** подзаголовок разбит на 2 строки — бренды через « · » без запятых («Wella · Londa · Schwarzkopf · Matrix · Lebel»), ниже отдельной строкой «Актуальный прайс и гиды по оттенкам и подбором под руку мастера.». Круглая рамка-«булька» вокруг лого убрана по просьбе пользователя — лого встык на красном фоне херо (цвета почти совпадают, край JPG практически не виден). Анимация: пытался реально векторизовать логотип для покадровой обводки линий («прорисовка») — оба доступных инструмента подвели (`vtracer` падает сегфолтом на Python 3.14 в этом окружении, чистый `potracer` дал грязный/неполный контур из-за JPEG-артефактов на тонких линиях) — вместо этого логотип «проявляется» через анимированную CSS-маску (диагональный wipe, `mask-image`+`mask-position`, 2.1s, `Hero.astro`) поверх реального изображения, без потери качества картинки. Если пользователь всё же хочет честную покадровую обводку — понадобится либо чистый SVG-исходник логотипа от пользователя (не JPG), либо более щедрый бюджет времени на ручную трассировку.

**Херо v4 (2026-08-16, отказ от img):** пользователь явно попросил НЕ вставлять `<img>` — хочет ту же технику, что на `maracuia.netlify.app` (референс сохранён у пользователя в `D:\Projects\test_site2`, флаг открывать при необходимости сверки: там `js/main.js` процедурно строит SVG-элементы цветка и анимирует их через `stroke-dashoffset`/`scaleY`+`transform-origin` со сдвигом по времени на элемент, см. класс `.petal-shape`/`.corona-line`/`filamentGrow`/`sway` в `css/styles.css`). Скопировал этот же приём в `Hero.astro`: реальный inline `<svg>` (профиль головы + расчёска с зубцами + ножницы + 3 пряди волос), никакого `<img>`. Пути «дорисовываются» через `path.getTotalLength()` + анимацию `stroke-dashoffset`, зубцы расчёски растут через `scaleY(0)→1`, пряди волос после отрисовки продолжают покачиваться (`strand-sway`, как `sway` у цветка). **Важно понимать ограничение:** это НЕ трассировка настоящего заказного логотипа Zilma (что не получилось, см. v3 выше) — это моя собственная упрощённая SVG-иллюстрация в том же духе (голова+инструменты), нарисованная и анимированная вручную с нуля. Если пользователь хочет именно свой логотип как реальный вектор — нужен исходный векторный файл (AI/EPS/SVG) от дизайнера, который делал лого, тогда можно будет анимировать уже точную форму.

**Херо v5→v9 (2026-08-16, ФИНАЛЬНАЯ версия — v5-структура, ОКОНЧАТЕЛЬНО ПОДТВЕРЖДЕНА пользователем, «отлично, сохрани» — дальше не трогать без явной новой просьбы)** — hand-drawn SVG (v4) отклонён пользователем («не годится» — ножницы перечёркивали лицо, зубцы расчёски не шли по дуге). Вернули `<img src="/images/logo.jpg">` в круглой «бульке» (box-shadow-кольцо) — настоящую покадровую векторную обводку логотипа сделать не вышло (см. v3/v4), пользователь принял это. Три стадии подряд:
1. **Coin-flip** (0–1.2s): `.bubble` крутится `rotateY(0→360deg)` — эффект монеты, `perspective:900px` на `.hero-art`.
2. **Фон целиком уходит в синий** (1.3–2.4s): `.bg-blue` — отдельный слой `position:absolute;inset:0` внутри `.hero` (НЕ внутри `.hero-art`!), `linear-gradient(135deg, --color-blue-dark, --color-blue)`, просто `opacity: 0→1` за 1.1s. Одновременно `.halo` — отдельный небольшой слой ВНУТРИ `.hero-art` (`inset:-9%` от бульки — сжато по разметке пользователя на скриншоте, красная линия почти вплотную к краю круга; до этого пробовали `-22%`, `-70%` и один гигантский растущий круг (4000px) — всё, что шире `-9%`, пользователь отклонял один за другим: «не то»/«ещё больше испортил»/«верни как было»/«сделай переход ближе к краю»), мягкое белое свечение вокруг лого: `radial-gradient(circle, rgba(255,255,255,.92) 0%, rgba(255,255,255,.28) 34%, transparent 56%)`, `opacity:0→1` за 1.1s. **Это ТОЧНЫЕ значения, зафиксированные и подтверждённые пользователем 2026-08-16 после многих кругов правок — не увеличивать inset/стопы без явной новой просьбы. Тенденция всех правок этого дня — пользователь последовательно сжимал ореол, а не расширял; если попросят ещё раз "мягче/шире", уточнить, действительно ли имеется в виду больше по площади, или что-то другое (плавность без изменения размера).**
3. **Wipe-reveal на логотипе** (2.4–4.5s): диагональная mask-анимация `zilma-wipe` (`mask-image`+`mask-position`) — единственная часть, дожившая без изменений от v3 до текущей версии.
4. **Кольцо вокруг бульки** — `.bubble`'s `box-shadow: 0 0 0 6px rgba(250,243,234,0.32), 0 20px 40px rgba(0,0,0,0.35)`. Было `0.15` (почти незаметно) → пробовали `0.85` (пользователь: «нет, не белее, а чуть светлее») → сошлись на `0.32`. Финальное значение, не менять без просьбы.

**Грабли, на которые наступили при экспериментах v6/v7 (для истории — в текущей v8 их уже нет, т.к. вернулись к двухслойной v5-структуре):**
- **Проценты/vmax в radial-gradient стопах при большом финальном размере не работают:** если размер элемента анимировать до огромного значения (пробовали `340vmax`), видимая область (херо) — это исчезающе малая доля от всего градиента, в кадре остаётся только цвет из первых процентов (белый центр) — фон выглядит "выцветшим". Даже после фикса на жёсткие px (4000px) сама идея одного растущего круга не понравилась пользователю визуально — с этим фиксом или без, подход отклонён.
- **`rgba(..., alpha<1)` в стопах градиента, лежащего поверх непрозрачной подложки другого цвета, даёт паразитный оттенок** там, где в конкретной точке экрана градиент ещё не дошёл до непрозрачного цвета (просвечивает то, что под ним). У `.halo` (текущая версия) это не проблема, т.к. под ним к моменту анимации уже лежит непрозрачный `.bg-blue`, а не голый красный `.hero` — если когда-то будете класть полупрозрачные белые стопы НЕПОСРЕДСТВЕННО на красный фон (до того как `.bg-blue` дошёл до `opacity:1`), проверить синхронизацию таймингов обоих слоёв, иначе будет розовое пятно как в v6.
- **Stacking context ловушка:** `.hero-art` получает свой stacking context из-за `perspective` (нужен для coin-flip). Из-за этого ВЕСЬ `.hero-art` (включая разросшийся `.bg-wash`) красится ПОВЕРХ соседнего `.hero-text` по порядку в DOM, даже если у `.bg-wash` z-index ниже, чем у `.bubble` — z-index внутри чужого stacking context не может вытащить элемент выше СВОЕГО контекста. Текст просто пропадал под фоном. Чинится явным `z-index` на `.hero-text` (2) и `.hero-art` (1) — оба ребёнка ОДНОГО общего родителя (`.hero-inner`, у которого уже есть свой context), тогда порядок между ними подчиняется z-index напрямую.
- Белая зона градиента изначально была слишком широкая (стопы 0/38/66%) — текст попадал в неё и терял контраст. Сузили до 0/10/24% — но пользователю это не понравилось («было лучше, верни как было, но без чёткого края») — вернули широкие мягкие стопы (0/12/26/46%), а контраст текста решили ИНАЧЕ: `.hero-text` уже имеет `z-index:2` (см. выше), сам текст сидит близко к логотипу лишь частично попадая в светлую зону, реальной проблемы с читаемостью на практике не возникло при широких стопах — если возникнет, регулировать стопы, а не сужать их обратно без необходимости.
- **`rgba(..., alpha<1)` в стопах радиального градиента, лежащего НЕ на непрозрачной подложке, даёт паразитный цвет по краям.** `.bg-wash` рисуется поверх красного `.hero`, и первая версия использовала `rgba(255,255,255,0.95)`/`rgba(255,255,255,0.5)` для «мягкости» — красный фон просвечивал сквозь полупрозрачные стопы везде, где в конкретной точке экрана градиент ещё не дошёл до непрозрачного цвета, из-за чего в углу херо (не в центре, а именно на периферии, куда `.bg-wash` уже дотягивается, но ещё в светлой части своих стопов) вылезло розово-лиловое пятно — не сразу очевидно, выглядело как логическая ошибка с размером/анимацией, но на самом деле чистая проблема alpha-прозрачности. Чинится: **все стопы radial-gradient делать полностью непрозрачными** (`#fff`, `#eaf1f7` и т.д., не `rgba(...,<1)`), «мягкость» перехода регулировать количеством/расположением стопов, а не прозрачностью.

**Структура (по `структура сайта.jpg` + описанию пользователя):**
- Херо (узкий): слева H1 «Профессиональная косметика для салонов» + подзаголовок с брендами, справа — логотип с анимацией. **Актуальная реализация и финальные значения — см. «Херо v5→v9» выше, не здесь.**
- Блок 1/2 (одинаковый размер): скачать текущий прайс / прайс с акциями + контакты для заказа (Telegram, WhatsApp, Bip, почта — у обоих блоков одинаковые). Данные и контакты — в `src/data/prices.ts`, обновление прайса = заменить файл в `public/prices/` и поправить дату в этом файле, больше ничего трогать не нужно.
- Фильтр-табы над лентой: Все / Цены / Акции / Окрашивание / Уход (`FilterTabs.astro`, чистый JS без фреймворка).
- Лента статей: самая новая — крупная карточка с пометкой «Новое» (вычисляется автоматически по `publishDate`, вручную флаг не проставлять), остальные — сеткой по датам по убыванию.
- Карточки товаров для блока акций (изображение/название/цена) — пока не заведены, поле `promoItems` в `src/data/prices.ts` пустое, готово для заполнения когда появятся акции.

**Технически:**
- Стек — Astro 7 + Markdown-контент (content collection `articles`, схема в `src/content.config.ts`), пользователь выбрал сам через сравнение вариантов.
- Статьи мигрируются из Telegraph-статей канала (не из короткого tema.md-тизера) конвертером `scripts/telegraph-to-md.py <telegraph-path>` → вставить в `src/content/articles/<та-же-дата-что-в-NEWS>-<line>.md`, картинки (обложка/палитра) скачивать в `public/images/articles/<id>/`. Обложки в статьях — переиспользуем уже готовые `_cover_preview.png` из NEWS, если подходят по стилю; при необходимости обложку можно пересобрать по методу из `[[brand-pdf-to-telegram-workflow]]`.
- **Мигрировано 3 из 13 статей** (Koleston Perfect, Color Touch, Illumina Color). Остальные 10 — по этому же образцу.
- **content.config.ts требует `description` в frontmatter** (не только title/brand/line/tags/coverImage/publishDate) — забыл его при первой попытке для Color Touch, `astro build`/`dev` падает с `InvalidContentEntryDataError` (dev-сервер при этом просто "не поднимается" без явной причины в обычном логе — реальную ошибку показывает только `astro build`, не `astro dev`, если сервер не стартует без видимой причины — гонять `astro build` для диагностики, не гадать).
- **Color Touch (2026-08-17) — метод «полное содержание PDF + картинки ровно как в файле»**, применённый по прямой просьбе пользователя ("должно быть полноценное инфо с картинками, повторять pdf"), задаёт общий стандарт для всех оставшихся 11 статей, не только Color Touch:
  - Источник — не Telegraph-статья (та короче), а **оригинальный PDF бренда** из `work\Работа\<бренд>\` (для Color Touch — `WELLA_COLOR_TOUCH_FINAL.pdf`, 2 страницы). Рендерить страницы в PNG и **реально прочитать их глазами** (Read tool), не полагаться на текстовый слой — так нашлось намного больше деталей (4 подварианта линейки: Plus/Relights/Sunlights/Instamatic), чем было в старой Telegraph-версии (только базовая линия + Plus).
  - **Извлечение картинок из PDF — только через RGB+SMask, не через сырой `doc.extract_image()` и не через `page.get_pixmap(clip=rect)`:** сырой JPEG-поток теряет альфу (даёт чёрную подложку вместо прозрачности), а обрезка по `rect` из `get_image_rects()` может задеть соседний контент страницы (тот же rect иногда шире визуального силуэта товара). Рабочий рецепт: `doc.extract_image(xref)` → если есть `info['smask']`, извлечь его тоже, `Image.open(...).putalpha(mask)`, затем `.crop(im.getbbox())` чтобы обрезать по границе непустых пикселей. Дальше как обычно — товарные фото по этому рецепту идут прямо в обложку (тем же способом v2: цвет с фото пипеткой, градиент, товар в белой трети) и инлайн в тело статьи.
  - Картинки вставлять **в тот же markdown-раздел, где эта тема встречается в PDF** (напр. фото Color Touch Plus — сразу в начале раздела "## Color Touch Plus"), не все скопом в конце.
  - Декоративные микро-иконки PDF (пиктограммы таймера/пропорций, галочки в сетке "область применения") — НЕ вытаскивать как картинки, переводить в обычный текст (уже опробовано на Koleston, подтверждено здесь же). Реальные товарные фото — вытаскивать всегда.
- Дев-сервер: `npm run dev` в папке `Site`, порт 4321. **Если после правки `.astro`-компонента в браузере не применяются новые CSS-правила (особенно составные селекторы вида `.card.kind .child`) — не гадать и не тратить время на отладку селекторов, сразу перезапустить dev-сервер** (`pkill`/`taskkill` node-процесса Astro + `npm run dev` заново). Один раз уже ловили: скомпилированный стиль на странице реально не содержал новые правила (проверено через `document.styleSheets` в JS) при абсолютно корректном исходнике — просто протухший HMR-кэш, перезапуск чинит сразу. **Осторожно:** `taskkill /IM node.exe /T` убивает ВСЕ node-процессы в системе, включая MCP-серверы на node (`duckduckgo-search`, `playwright`, `pollinations`, `telegram-bot` из `work/.mcp.json`) — если они понадобятся в той же сессии, их нужно будет переподключить (новая сессия Claude Code сама переподключит через `.mcp.json` при старте). Лучше сначала попробовать убить только сам `astro dev` процесс точечно, а не все node сразу, если получится.
- Реальные прайсы получены от пользователя 2026-08-16 — оказались **`.xlsx`, не `.pdf`** (лежали в `D:\Projects\work\Site\Price\2026-08-14-price-list.xlsx` и `2026-08-14-promotions.xlsx`, дата в имени файла — реальная дата актуальности, не дата получения). Скопированы в `public/prices/price-current.xlsx` и `price-promo.xlsx`, `src/data/prices.ts` обновлён под `.xlsx` и дату `2026-08-14`. Кнопка скачивания теперь подписывается по расширению файла динамически (`Скачать {EXT}` в `PriceBlock.astro`) — если в следующий раз пришлют PDF, ничего в коде менять не придётся, подпись поменяется сама.

- **ТРИ КНОПКИ ГЛАВНОГО ПРАЙС-БЛОКА — НЕПРИКОСНОВЕННАЯ ТРОЙКА, установлено 2026-08-25 (реальный инцидент, пользователь чуть не сорвался).** «Каталог — выбрать и заказать», «Акции — выбрать и заказать», «Скачать EXCEL» (`PriceBlock.astro`, кнопки внутри `kind==='current'`) — все три должны существовать на сайте ВСЕГДА и ВСЕГДА обновляться вместе при любом пересчёте прайса. **Что произошло:** пользователь попросил убрать кнопку «Акции — выбрать и заказать» отдельным сообщением ("Удали все что связано с кнопкой") — я убрал её (и пункт «Акции» из мобильной плашки) и запушил в прод без предварительного показа пользователю. Через минуту пользователь увидел живой сайт и был в ярости («чуть до инфаркта не довёл») — оказалось, реальное намерение было другим (или пользователь передумал, увидев результат вживую — не суть важно), кнопка нужна. Откатил коммит (`git revert`), кнопка и пункт плашки восстановлены, задеплоено обратно в течение нескольких минут.
  - **Правило на будущее #1 — НИКОГДА не пушить в `main` изменения, влияющие на видимые элементы сайта (кнопки, блоки, навигацию), без явного «да, публикуй» от пользователя в ЭТОМ диалоге**, даже если инструкция на удаление казалась однозначной. Показать локально/скриншотом — да, всегда. Запушить сразу — только если пользователь прямо это подтвердил именно для этого изменения (одобрение чего-то другого раньше не считается).
  - **Правило на будущее #2 — если правишь скрипт формирования прайса (`build_price_current.py`, `xlsx-to-price-items.py`) — обнови и проверь все три кнопки, не только файл.** Каталог и Акции — из `src/data/priceItems.json` (обе кнопки читают один и тот же файл, Акции — то же самое с фильтром `promo:true`); Скачать EXCEL — из `public/prices/price-current.xlsx`. Отдельный файл `price-promo.xlsx` НЕ формируется (см. следующий пункт) — это осознанное отдельное решение, никак не связанное с самой кнопкой «Акции»: кнопка использует общий JSON, а не этот файл, и остаётся на сайте независимо от того, генерируется отдельный акционный excel-файл или нет.
- **Методика формирования `price-current.xlsx` — решено 2026-08-25.** Из пары исходников (продажные цены + остатки/себестоимость, кладутся в `Site\Price\`) один скрипт `Site\Price\build_price_current.py` считает акции по марже (не по наценке): маржа% = (цена−себестоимость)/цена, скидка по ступеням — маржа≥45%→15%, 30-45%→7%, <30%→0%/не в акции. Структура/оформление 1С сохранены (бренд-заголовки, объединённые ячейки), акционные строки перекрашены в цену со скидкой и выделены красным, шапка ценовой колонки переименована с внутреннего 1С-лейбла (напр. «2. Олег») на «Цена». Исключены служебные строки-не-товар: `доставка 007927` (по имени/артикулу) и секции-«клиренсы» `Wella_Акция`/`Londa_Акция` (мятый/бракованный товар без цены — эти строки раньше ошибочно принимались за заголовки бренда из-за отсутствия цены и портили группировку бренд/линейка у всех товаров после них; чинится проверкой "нет цены И нет единицы измерения = заголовок", см. `EXCLUDE_NAME_PREFIXES`/`EXCLUDE_ARTICLES`/`JUNK_SECTION_SUFFIXES` в скрипте). **Отдельный файл `price-promo.xlsx` больше не генерируется** — это не то же самое, что кнопка «Акции» (см. пункт выше про неприкосновенную тройку): раньше был отдельный акционный excel для скачивания, его убрали как чистое дублирование данных, но интерактивная кнопка «Акции — выбрать и заказать» (окно с фильтрованным списком) осталась и никак не зависела от этого файла. **Обязательно перед публикацией: пользователь сам проверяет получившийся файл — не пушить/не деплоить как «обновление прайса» без его явного подтверждения.** Полная методика — в памяти [[project-zilma-promo-price-methodology]] (`D--Projects-work`, не в этой папке — см. ниже про память по cwd).
- **Баг с «наездом» строк в `price-current.xlsx` — найден и исправлен 2026-08-26, уже вшит в скрипт, повторно не диагностировать.** `ws.delete_rows()` в openpyxl двигает значения ячеек, но НЕ переносит высоты строк (`row_dimensions`) и объединения C:D (заголовки брендов/линеек) на новые номера строк — они остаются привязаны к старым номерам. После удаления мусорных строк это давало реальную визуальную кашу (поймано пользователем на скриншоте): длинное название товара получало высоту от короткой соседней строки и текст наезжал на следующую строку, а объединение цена+единица могло вообще оказаться на строке товара вместо заголовка бренда. **Починено функцией `remove_rows_clean()`** в `build_price_current.py` — строит карту старый→новый номер строки, сохраняет высоты и объединения по этой карте, снимает все merge, удаляет строки, накатывает высоты/merge обратно на правильные новые позиции. Проверено программно на всём файле (не выборочно): 1183 из 1183 строк и все 31 объединение совпали с оригиналом. При обычном перезапуске скрипта это уже не всплывёт — актуально только если кто-то будет переписывать `build_price_current.py` и вернёт голый `ws.delete_rows()`, либо если появится другой скрипт, удаляющий строки из копии реальной 1С-книги (то же ограничение openpyxl применимо и там).
- **«Осиротевшие» заголовки без единого товара — найдено и исправлено 2026-08-26.** Если у бренда/линейки исключили единственный товар (напр. был мятым), сам заголовок раньше оставался пустым обрубком в прайсе (реальный случай: `REFECTOCIL` перед `SCHWARZKOPF`, без единой позиции). Теперь скрипт после всех исключений отдельно ищет заголовки без товаров под ними и убирает их тоже — **но осторожно: область бренда считается до СЛЕДУЮЩЕГО бренда, а не до любого следующего заголовка**, иначе бренд, где товары есть только под его собственными линейками (напр. `MATRIX` → `Total Results New` → товары), ошибочно считается пустым и чуть не улетел целиком при первой попытке этой правки. Различение бренд/линейка — по заливке ячейки, та же логика (`indexed=8`/`22`), что уже использует `xlsx-to-price-items.py`. Подробности и разбор первой неудачной попытки — в памяти [[project-zilma-promo-price-methodology]].
- **Колонка «Заказ» в `price-current.xlsx` — добавлена 2026-08-26, постоянная часть пайплайна.** Пятая колонка (E), пустая, с той же сеткой границ и заливкой заголовков, что и вся таблица (не голая колонка) — клиент вписывает туда количество при заказе по скачанному файлу. Только в xlsx для скачивания, `priceItems.json`/интерактивный каталог не трогает (там уже есть свой степпер количества). **Грабли:** правка `border` у НЕ-якорной ячейки объединения (напр. `D` в `C:D`) молча не сохраняется при записи файла — чинится через `unmerge_cells()` (сначала скопировать старый border, потом снять объединение, только потом менять border). Подробности — в памяти [[project-zilma-promo-price-methodology]].
- **Интерактивный каталог — отдельный шаг пайплайна, не забывать.** Обе кнопки-диалога (`PriceChecklist.astro`, «Полный прайс» и «Акции») берут данные не из xlsx напрямую, а из `src/data/priceItems.json` — после `build_price_current.py` обязательно ещё прогнать `python Site/scripts/xlsx-to-price-items.py` (читает `price-current.xlsx`, включая заливку ячеек заголовков: `indexed=8` чёрный = бренд/«папка», `indexed=22` серый = линейка/«субпапка» — то же деление в стилях `PriceChecklist.astro`), иначе список товаров в обоих окнах останется старым. Красный шрифт и бейдж «АКЦИЯ» — через флаг `promo` в JSON.
- **Выбор удобного мессенджера в форме заявки — добавлено 2026-08-25.** Причина: часть клиентов без VPN пугалась слова «Telegram» в поле телефона (плейсхолдер уже упростили до просто «Телефон», см. пункт про 2026-08-16 выше). Под телефоном — необязательные чипы `Telegram`/`WhatsApp`/`MAX`/`Bip` (`OrderForm.astro`, `.messenger-chips`, в стиле `.tab` из `FilterTabs.astro`): клик выделяет один, повторный клик снимает выбор, значение уходит в скрытый `<input name="messenger">` и попадает в заявку (и Telegram, и WhatsApp best-effort канал, `api/order.ts`) отдельной строкой «Удобный мессенджер: …», только если выбран — если нет, строка не показывается вообще. **На мобильном чипы не переносятся на вторую строку** (`Bip` уезжал вниз на узких экранах) — `.messenger-chips` на `flex-wrap: nowrap` + `overflow-x: auto` (прокрутка вместо переноса, скроллбар скрыт), плюс `.chip` сжимается (меньше padding/font-size) под 420px. Проверено вживую на 320px — всё в один ряд.
- **Память этого проекта иногда лежит в другой папке — проверять при странностях.** Память по умолчанию хранится по пути текущей рабочей директории сессии (`~/.claude/projects/<encoded-cwd>/memory/`). Если сессия хоть раз была запущена с `cwd = D:\Projects\work\Site` (а не `D:\Projects\work`) — её память ляжет в отдельную папку `D--Projects-work-Site`, не в основную `D--Projects-work`. Реальный случай 2026-08-25: пользователь переделывал сайт в сессии из `Site`, решение там не долетело до основной памяти проекта. Если пользователь ссылается на решение/договорённость, которых не находится в текущей памяти — прежде чем говорить «не помню», проверить соседнюю папку `D--Projects-work-Site`.
- Иконки блоков — пользователь прислал `блок1.png` (документ с карандашом, для текущего прайса) и `блок2.png` (документ с галочкой, для акций), лежат в `public/images/blocks/block-current.png` / `block-promo.png`, показываются под датой в статист-панели каждого блока.
- Пастельные фоны статист-панелей — по разметке пользователя на скриншоте: `current` → `#bcd8f2` (пастельно-голубой), `promo` → `#f3c1c9` (пастельно-розовый/красный), заданы в `PriceBlock.astro` через `.price-card.current .stat-panel` / `.price-card.promo .stat-panel`.
- Контакты в обоих блоках — иконки (не текстовые подписи): все 4 (telegram/whatsapp/bip/email) — инлайн SVG внутри `PriceBlock.astro`, рисуются вручную под `currentColor` (не растровые файлы — растровые версии bip пробовали дважды, оба раза не подошли: мутные в 18px и на hover цвет иконки совпадал с цветом фона, становясь невидимой). Bip нарисован по контурному референсу пользователя (круг-пузырь с хвостиком + буквы «bip» слитным контуром, из простых path/line/circle, см. компонент). Текстовые label/value остались в `aria-label`/`title` для доступности, визуально не показываются. Ссылка на Bip (`https://dl.bip.com/egbuXYcQ`) в `src/data/prices.ts` верная.
- **`verifiedDate` — обязательно поднимать при ЛЮБОМ содержательном изменении статьи, не только текста.** Реальный промах 2026-08-23: заменил картинки (обложки, товарные фото, палитры) у всех 4 статей, но забыл поднять `verifiedDate` — на карточках главной даты остались старые (17-18 августа) хотя контент реально обновлялся 23-го. Пользователь сам это заметил и поправил. Правило: закончил правку контента ЛЮБОГО типа (текст, картинка, палитра, обложка) → в том же заходе поставить `verifiedDate: {сегодня}` в frontmatter статьи, до коммита, не потом. Дата на главной берётся из content collection — после правки frontmatter обязательно перезапустить dev-сервер (обычный HMR её не подхватывает, та же природа, что и с CSS, см. ниже), иначе визуально покажется что не сработало.
- **Оптимизация картинок статей — обязательный шаг, не пропускать (заведено 2026-08-22).** Картинки из PDF/скриншотов приходят полноразмерными (палитры бывали до 4096px шириной при показе на сайте всего 480px высотой) — прогонять каждую через `python scripts/optimize-image.py <категория> <файл>` (категории `cover`/`product`/`palette`/`icon`, таблица размеров — в README.md, раздел «Оптимизация картинок»), закоммитить получившийся `.webp`, исходный PNG/JPG удалить, ссылки в markdown/frontmatter поправить на `.webp`. На первом прогоне (4 готовые статьи, 18 файлов) дало 9.5 МБ → 1.3 МБ без потери качества. Это НЕ встроено в сборку сайта (не `astro:assets`, просто ручной скрипт) — шаг нужно выполнять руками/мной при каждой новой статье или замене картинки, само не сработает.
- **Поиск актуальных товарных фото/упаковки для картинок статей — порядок источников (заведено 2026-08-22, при пересборке Color Touch).** Сначала официальный сайт бренда (напр. wella.com) — там обычно только маркетинговые лайфстайл-баннеры, а не чистые фото товара на белом фоне для конкретного оттенка/варианта линейки. Если официального сайта недостаточно — идти в авторизованный официальный магазин бренда (для Wella это WellaStore, по регионам, напр. `uk.wella.professionalstore.com`) — у него в карточке товара обычно есть чистое фото на белом + лайтбокс с зумом (кнопки +/− под картинкой), берётся через `mcp__claude-in-chrome__computer` (`zoom` action с `save_to_disk:true`) — разрешение скромное (обычно 150-400px по стороне, это потолок инструмента скриншота, не сайта), но дизайн упаковки подтверждён напрямую от бренда. **Если и там нужного ракурса/элемента набора нет — тогда и только тогда переключаться на широкий поиск в интернете (WebSearch/поиск картинок), но подбирать результат СВЕРЯЯ его с уже подтверждённым на официальном источнике дизайном** (не брать первое попавшееся фото с абстрактно похожим названием — упаковка бренда часто отличается по регионам/годам). Если для конкретной статьи нужно несколько предметов вместе (напр. флакон + коробка) и официальный источник даёт только фото каждого предмета по отдельности — собирать композицию из отдельных фото самому (PIL), не считать это поводом идти в широкий поиск.

**Домашнее задание (см. раздел про CLm02 выше):** сайт = проект для варианта 3. Деплой ещё не делали — куда именно (Vercel рекомендован — свободный git-деплой, автоматический SSL, ничего общего с возней вокруг Beget/DNS) нужно подтвердить с пользователем перед деплоем.

# Дизайн-система гид-виджетов статей (2026-08-17, эталон — Koleston Perfect)

Пользователь попросил повторить оформление официальных PDF-инфографик Wella (пропорции смешивания, круговые «часы» выдержки, шаги аллерготеста, шкалы) **не картинкой-скриншотом, а настоящим контентом** — выбран «Вариант C»: свой набор CSS/HTML-виджетов в палитре сайта, а не копия фирменного дизайна Wella. Реализовано и подтверждено пользователем на статье `2026-08-13-koleston-perfect.md` — **это теперь стандарт для оставшихся 11 статей**, не изобретать заново, переносить как есть.

**Где лежит:** все стили и JS — в `Site/src/pages/articles/[id].astro` внутри `<style>`/`<script>` (общие для всех статей, т.к. это динамический `[id].astro`, не per-article файл). Контент-разметка — сырой HTML прямо в `.md` файле статьи (Astro пропускает raw HTML в markdown как есть).

**Набор виджетов (классы, все под `.prose :global(...)`):**
- `.wheel` (+ `.sm`, `.triple`) — кружок-«колесо» на `conic-gradient`, заливка `var(--color-pastel-red)`/`var(--color-pastel-blue)` (не насыщенный `--color-red`/`--color-blue` — см. ниже про палитру). `--split` (CSS-переменная, %) задаёт реальную долю — для пропорции смешивания (1:1 → 50%, 1:2 → 33%) и для времени выдержки (без тепла : с теплом). Не декоративно, а по факту, каждый раз считать реальное значение, не ставить наугад.
- `.mix-card` — крупная карточка: колесо-пропорция слева (растёт на всю высоту, `grid-row:1/3`), текст+список справа сверху, полоска-таймер (свой маленький `.wheel` + текст) снизу. Для разделов с несколькими % окислителя и списком эффектов (пример: «Схема смешивания с Welloxon Perfect»).
- `.service-card` — компактный вариант в один ряд: колесо + заголовок (+ `.ratio-tag` пилюля с пропорцией) + описание + время. Для разделов «один рецепт = одна карточка» (уходовые техники, глянцевание, пастельное тонирование).
- `.frac-list` — список-строки с маленьким `.wheel.sm` слева, для долей/пропорций без отдельного рецепта (пример: «добавить 1/3 объёма»).
- `.timer-chip` — инлайн-пилюля колесо+текст, для одиночных упоминаний времени вне карточки (пример: «Нанесение на отросшие корни»).
- `.step-flow` — сетка шагов с нумерованным `.badge`-кружком и фото/иконкой под ним (аллерготест). Если фото шага нет или оно битое — `.step-icon` (пунктирный плейсхолдер с коротким текстом) вместо `<img>`, не пропускать шаг молча.
- Таблицы (`table`/`th`/`td` — глобальный стиль, без класса) — автоматически получают оформление сайта (шапка `--color-panel`, чередующиеся строки), просто использовать обычный markdown-синтаксис таблицы.
- `.scale-row`/`.scale-bar` (`.depth`, `.tone`) — горизонтальная шкала-градиент вместо картинки-диаграммы, когда официальной картинки нет или она битая (пример: «Система нумерации оттенков» — 2 шкалы вместо несуществующего `numbering-system.png`).
- `.callout` — акцентная врезка (цветная левая полоса) для отдельных концептуальных определений без пропорции/времени (пример: «Дополнительный цвет», «Кривая осветления»).
- `.checklist`/`.check-icon` — ряд пилюль с галочкой для чекбокс-сеток «область применения» из официальных PDF-инфолистов Wella (пример: Color Touch, страница 1 — 5 колонок с зелёными ✓ под разными техниками). Один `<li>` = один официальный пункт, отмеченный галочкой в источнике — не добавлять пункты без галочки в PDF и не пропускать отмеченные.

**Лайтбокс (увеличение картинок по клику)** — общий для всех `.prose img`, JS в конце `[id].astro`: клик открывает картинку в оверлее на весь экран в реальном разрешении, второй клик закрывает. Добавлен, потому что палитра оттенков (`palette.png`, обычно ~2400px) нечитаема в узкой колонке статьи (760px) без этого. Работает уже на все статьи автоматически (общий файл), ничего дополнительно включать не нужно — просто не убирать `cursor:zoom-in`/`.lightbox-overlay` при правках стилей.

**Палитра для виджетов — только переменные из `global.css`, никогда не изобретать свой hex:** `--color-pastel-blue: #bcd8f2` / `--color-pastel-red: #f3c1c9` (те же, что фон стат-панелей в блоках прайса `PriceBlock.astro`) — заливка `.wheel`. `--color-red-dark` — сплошные акцентные значки (`.step-flow .badge`, `.checklist .check-icon`). **Реальный инцидент 2026-08-17:** для галочек `.checklist` был добавлен произвольный зелёный `#4d9463`, которого нет в палитре сайта — пользователь поправил («в палитре сайта его нет, не придумывай»), заменено на `--color-red-dark`. Урок: **перед тем как красить новый элемент — сверяться со списком переменных в `global.css` (`:root`), не подбирать "подходящий" цвет на глаз.**

**`ArticleCard.astro` — фон `.text-panel` теперь `var(--color-pastel-blue)` для ВСЕХ карточек ленты** (не только Koleston) — это глобальное изменение компонента, подтверждено пользователем 2026-08-17 по скриншоту. Если для конкретной статьи нужен другой фон текстового блока — потребуется отдельный вариант/проп, сейчас его нет.

**Обложка статьи (`coverImage` = один файл на карточку ленты + шапку страницы статьи) — стандарт решён 2026-08-17:**
- **Новый стандарт для всех статей** (подтверждено пользователем на Koleston, затем явно перенесено на Color Touch — «в таком же духе как колестон»): **просто фото товара на белом фоне, без градиента и без текста** (заголовок и так есть отдельным `<h1>` под обложкой на странице статьи). Товар крупный (высота ~590 из 630px канвы), с отступом сверху ~30px — без отступа при hover-zуме карточки (`transform:scale(1.05)` в `ArticleCard.astro`) верхушка флакона визуально «выходит» за рамку.
- Старый градиентный баннер v2 (цветной градиент слева + текст + товар справа, из `[[brand-pdf-to-telegram-workflow]]`) — **больше не используется для новых обложек**, применялся только для самых первых версий до 2026-08-17.
- `.cover-overlay` (тёмный градиент поверх обложки на странице статьи, в `[id].astro`) был почти чёрным (rgba до 0.55) — с белым фото это выглядело грязным серым пятном. Облегчён до едва заметной тени только у нижнего края (`transparent 75% → rgba(43,32,28,0.08) 100%`), чтобы не пачкать белый фон.
- Скрипт сборки обложки (белый канвас 1200×630 + product-фото по центру, PIL) — референс сохранён в `Site/_tmp_koleston2/` и `Site/_tmp_colortouch2/`, не удалять.

**Шапка страницы статьи (`[id].astro`, банер над `<h1>`) — отдельный от `coverImage` элемент, готово 2026-08-18:** раньше `.cover` показывал `coverImage` как фон (растянутое фото товара) — теперь фон однотонный/градиентный по полю `accent` (`content.config.ts`: `z.enum(['blue','red']).default('blue')`, обязательно проставлять в каждой статье), а поверх — лого-«булька» Zilma (тот же приём, что в `Hero.astro`: белый halo + кольцо box-shadow + `logo.jpg`), включая ту же анимацию проявления `zilma-wipe` (диагональный mask-wipe, 2.1s). **Карточка в ленте (`ArticleCard.astro`) не тронута** — там по-прежнему настоящее фото товара из `coverImage`.
- `accent` подтверждён для всех 4 готовых статей: Koleston Perfect → `blue`, Illumina Color → `blue`, Color Touch → `red`, Shinefinity → `red`. Для новых статей выбирать по цветовой ассоциации линии/бренда, не наугад.
- Точные финальные размеры/позиция (подобраны итеративно по разметке пользователя на скриншоте, не менять без явной новой просьбы): `.cover-art` — `width: clamp(180px, 20vw, 210px)`, `aspect-ratio: 1`. `.cover-inner` — `position:absolute; height:260px` (высота самого `.cover`), `display:flex; align-items:center; justify-content:flex-end` внутри `.container` (совпадает с правым краем контента, как `.hero-art` в Hero) — за счёт `align-items:center` отступы сверху/снизу от границ фона всегда одинаковые на любой ширине экрана, магических margin-top больше нет.
- `.cover-inner` — **сиблинг `.cover`, а не потомок** (обёрнуты в общий `.cover-wrap`), с `z-index:3` и `pointer-events:none`. Это намеренно: сам `.cover` держит `overflow:hidden` (чтобы не вылезал фон), но лого должно визуально ложиться поверх стыка баннера и белой карточки статьи (тонкий нахлёст на card ~20-25px) — если бы лого было внутри `.cover`, оно бы обрезалось по нижнему краю баннера. `pointer-events:none` — чтобы декоративный слой не перекрывал клики по ссылке «← Все статьи» и тексту под ним.
- Если появится третий акцентный цвет (не blue/red) — добавить оба места: enum в `content.config.ts` и класс `.cover-<цвет>` в `[id].astro`.

# Деплой — сделано (2026-08-18/22, задним числом зафиксировано здесь 2026-08-23)
Пункт 5 в «Открытых задачах» ниже был устаревшим («деплой ещё не делали») — реально задеплоено уже давно, просто этот файл не обновили вовремя. Факты:
- GitHub-репозиторий: `https://github.com/aiodoggmd/zilma-site` (аккаунт `aiodoggmd`, email `aiodoggmd@gmail.com`). Vercel подключен через «Continue with GitHub», импортирован тот же репозиторий, автодеплой при каждом `git push` в `main` — ничего вручную пересобирать/заливать не нужно.
- Сайт живёт на **`https://zilma-site.vercel.app`** и на собственном домене **`https://www.zilma.pro`** (тариф Vercel Hobby, бесплатно). **Важно: канонический хост — `www.zilma.pro`, не голый `zilma.pro`** — Vercel 308-редиректит апекс-домен на `www` (так настроено в Vercel Domains), поэтому все SEO-теги (`astro.config.mjs` → `site:`, `robots.txt`) должны указывать на `www.zilma.pro`, иначе краулеры получают лишний редирект-хоп. Найдено и исправлено 2026-08-23.
- Домен `zilma.shop` (был в процессе настройки через Cloudflare DNS у Hostinger) — заброшен, заменён на `zilma.pro`. Подробностей самого перехода в истории сессий не осталось, если пользователь упомянет `zilma.shop` — не путать, это прошлое.
- **Рабочий процесс с этой сессии: пуш в `main` = мгновенный деплой на прод**, отдельного стейджинга нет. После первого явного «да, запушить» от пользователя в сессии 2026-08-23 — при доработках, которые нужно тут же проверить вживую (теги, картинки, манифест и т.п.), пушить сразу после коммита и билда без ожидания повторного разрешения на каждый пуш; для более рискованных/необратимых изменений уточнять отдельно.
- **Как проверять, что задеплоенная версия реально обновилась** (сессия 2026-08-23, наработанный приём): `curl -s https://www.zilma.pro/... | grep ...` в цикле с `sleep 15` (пропуск деплоя занимает обычно 15-40 сек) — ждать нужную строку в ответе, не гадать по таймауту. Для картинок/бинарных файлов сверять не глазами (превью в инструменте чтения может закешироваться и показать старую картинку даже когда файл на диске уже новый — поймано на `og-default.jpg`), а **побайтово через хеш** (`md5sum` или `hashlib.md5` в Python) локального файла и скачанного через `curl` с домена.

# SEO, фавикон/иконки, PWA-установка (2026-08-23)

**SEO-теги (`src/layouts/BaseLayout.astro`).** Добавлены: `canonical`, полный Open Graph (`og:type/site_name/locale/title/description/url/image(+width/height)`, `article:published_time`/`modified_time` для статей), Twitter Card (`summary_large_image`), JSON-LD (`Organization` на главной, `Article` на страницах статей — даты publish/modified, `headline` полный, не обрезанный). `astro.config.mjs`: `site: 'https://www.zilma.pro'` + интеграция `@astrojs/sitemap` (генерит `sitemap-index.xml` автоматически при билде, ничего вручную поддерживать не нужно). `public/robots.txt` со ссылкой на sitemap.
- **`og:title`/`twitter:title` для статей — отдельный от `<title>` проп `ogTitle`** (см. `BaseLayout.astro`): полные заголовки статей (80-102 символа с суффиксом «— Zilma») обрезались на середине слова в превью Telegram/Twitter. `[id].astro` считает `ogTitle` через `truncateTitle()` (~70 симв., режет по границе слова, без brand-суффикса — домен и так виден в превью), `<title>` вкладки и JSON-LD `headline` остаются полными.
- **`og:image` (`public/images/og-default.jpg`, 1200×630)** — своя картинка для шаринга, не переиспользует `logo.jpg`/фавикон напрямую (те не в пропорции 1.91:1). Текущая версия (после смены дизайна лого, см. ниже): синий градиент (`--color-blue-dark`→`--color-blue`, как в Hero) + белый halo + новая иконка Zilma + подпись «ZILMA» (Georgia Bold — сайтового Playfair Display как системного шрифта нет, для мелкого текста на картинке разница не критична). Статьи используют свой `coverImage` как `og:image` вместо общей картинки.
- **`/order/` (`src/pages/order.astro`)** — отдельная страница для потока «Выбрать и заказать» (тот же `PriceBlock`+`PriceChecklist`+`OrderForm`, что и в блоке на главной), со своими `title`/`description` для шаринга — раньше у этого CTA не было своего URL, поэтому и своего превью при пересылке ссылки в мессенджер тоже не было.
- **Кеш-бастинг на статичных иконках/картинках (`?v=N` в query у `<link>`/`<meta>` в `BaseLayout.astro`).** Браузеры и соцсети (Telegram/Facebook/Twitter) кешируют favicon и og:image агрессивно и по URL, независимо от `Cache-Control` файла — при замене картинки без смены URL получатель может ещё долго видеть старую версию. Каждый раз при реальной смене содержимого favicon/og-картинки — поднимать `?v=` на 1 у соответствующих ссылок в `BaseLayout.astro`, не просто менять файл на диске.

**Региональные SEO-сигналы для российской аудитории — добавлено 2026-08-26.** Аналитика (Vercel Web Analytics) показала, что зарубежный трафик (США 24%, Британия 21%) заметно больше российского (16%) — при том что бизнес чисто российский (доставка по Москве/регионам). Без логина пользователя добавлено: `<meta name="geo.region" content="RU-MOW">` + `<meta name="geo.placename" content="Москва">` в `BaseLayout.astro`, `areaServed: 'RU'` + `address` (Москва, RU) в Organization JSON-LD (`index.astro`). Это лёгкие/вспомогательные сигналы, не главный рычаг. **Главный рычаг — Яндекс.Вебмастер, который требует логина пользователя и НЕ подключён** (см. пункт про Google Search Console/Яндекс.Вебмастер в плане ниже — актуален по-прежнему, инструкция для пользователя выдана в чате 2026-08-26, не в этом файле). В России Яндекс даёт основную долю поискового трафика — без него региональные метатеги выше просто вспомогательные, основной эффект даст только сам Вебмастер (там же указывается регион «Москва» явно).

**Фавикон/иконки приложения — три итерации за сессию, финал (2026-08-23):**
1. Обнаружено: на сайте всё это время был дефолтный плейсхолдер-фавикон Astro (шаблонный, никогда не заменялся) — показывался как невнятная иконка вкладки.
2. Первая замена: простой круг `--color-red` + белая буква «Z» (сгенерирован кодом, PIL).
3. **Финал (текущая версия):** пользователь прислал свой AI-сгенерированный (через GPT) дизайн — тёмно-красный квадрат со скруглёнными углами, белый «Z» с силуэтом женского профиля в причёске. Оригинал 1254×1254 лежит в **`Site/favicon.png`** (в корне проекта, НЕ в `public/` — не удалять, это мастер-исходник для пересборки любых размеров иконок в будущем). Белые уголки-вырезы (декоративное скругление самой картинки) залиты фоновым красным через `ImageDraw.floodfill` от всех 4 углов (`thresh=130`) — без этого iOS/Android накладывали бы свою маску поверх уже скруглённого изображения и было видно белые огрызки по углам.
   - Сгенерированные из мастера файлы: `favicon.ico` (16/32/48/64, настоящий multi-size ICO, не PNG-переименовашка как было раньше), `favicon.svg` (embedded base64 PNG 256px — дизайн фотографический/с мелкими деталями, честную векторную трассировку делать не пытались, не тот случай), `favicon-32x32.png`, `favicon-16x16.png`, `apple-touch-icon.png` (180, full-bleed квадрат — iOS сам накладывает маску), `icon-192.png`/`icon-512.png` (purpose:any, версия **круглая** — см. ниже почему), `icon-192-maskable.png`/`icon-512-maskable.png` (purpose:maskable, full-bleed квадрат с доп. отступом ~25% под «безопасную зону» адаптивных иконок Android).
   - **На 16px (вкладка браузера) детали причёски/лица неизбежно сливаются в силуэт** — ограничение самого детализированного дизайна, не баг конвертации, предупредить пользователя если ещё раз спросит.

**PWA-установка (site.webmanifest, apple-touch-icon, service worker, iOS-теги) — сделано 2026-08-23, все файлы в `public/`:**
- `site.webmanifest`: `name`/`short_name` «Zilma», `display: standalone`, `background_color: #38628c` (синий Hero — так же используется как фон PWA-сплэш-скрина при первом открытии установленного приложения, НЕ то же самое что обычный favicon), `theme_color: #c8000f`, иконки с явным `purpose` (`any` = круглые красные, `maskable` = квадратные с запасом). **Почему два набора:** если дать Android только круглую иконку без `maskable`, его адаптивная система иконок накладывает СВОЮ маску поверх уже круглой картинки (двойное скругление, белые огрызки по краям) — с явным `maskable`-вариантом этого не происходит.
- Service worker (`public/sw.js`, регистрируется в `BaseLayout.astro` через `navigator.serviceWorker.register('/sw.js')`) — простая network-first стратегия с офлайн-фолбэком на кеш для GET-запросов своего домена, **`/api/*` и не-GET запросы явно исключены** (заявки из `OrderForm` должны всегда идти живым запросом на сервер, не подменяться кешем). Технически с 2022-2023 (Chrome 108/112) service worker больше не обязателен для простой установки через меню браузера, но нужен для «богатого» автоматического диалога установки и для любой офлайн-устойчивости вообще.
- iOS-мета-теги (`apple-mobile-web-app-capable`, `-status-bar-style: default`, `-title: Zilma`) — без них ярлык на iOS открывался бы просто как вкладка Safari с адресной строкой, а не как отдельное приложение.
- **`src/components/InstallPrompt.astro`** — баннер снизу экрана (сайт-вайд, подключён в `BaseLayout.astro`), предлагает установку явно, а не полагается на то что пользователь заметит мелкую иконку в адресной строке Chrome:
  - Android/Chromium: слушает `beforeinstallprompt`, показывает кнопку «Установить», по клику вызывает системный диалог установки (`deferredPrompt.prompt()`). Эта кнопка — не своя логика, а обёртка над реальным браузерным API; появляется, только если браузер сам решил, что сайт достоин установки (это нельзя обойти кодом).
  - iOS (Safari **и** Chrome — на iOS Chrome тоже работает через WebKit по требованию Apple, у него нет `beforeinstallprompt`): показывает статичную инструкцию «Поделиться → На экран Домой» — единственный возможный вариант, программной установки на iOS не существует в принципе.
  - Скрыт, если уже запущено в standalone-режиме (уже установлено). Закрывается крестиком на текущую сессию браузера (`sessionStorage`, не `localStorage`) — при новом визите показывается снова, пока не установят.
  - **Ограничение, которое НЕЛЬЗЯ починить кодом:** уже установленные на телефон ярлыки/иконки не обновляются сами при смене дизайна — иконка «вшивается» в момент установки. Единственный способ увидеть новую иконку — удалить старый ярлык и установить заново. Предупреждать об этом пользователя каждый раз при смене фавикона/иконок после того, как он уже что-то устанавливал.

# Статус
- Астро-проект собран и работает локально (`npm run dev`, порт 4321).
- **Херо — ФИНАЛЬНАЯ версия, подтверждена пользователем («отлично, сохрани»), не трогать без явной новой просьбы.**
- Прайс-блоки, фильтр-табы (без «Цены», см. выше), секция «АКЦИИ И ИНФО» — визуально проверены, готовы.
- Мигрировано 4 из 13 статей на новую дизайн-систему (гид-виджеты + чистая обложка): **Koleston Perfect**, **Color Touch**, **Illumina Color**, **Shinefinity**. Порядок карточек в ленте (по `publishDate` с временем, чтобы не падать в алфавитный тай-брейк при одинаковой дате) — от старой к новой: Koleston → Color Touch → Illumina → Shinefinity (самая новая, бейдж «Новое»).
- **Сайт задеплоен и публично доступен: `https://www.zilma.pro`** (детали — раздел «Деплой — сделано» выше).
- **SEO-теги, фавикон/PWA-иконки и установка сайта как приложения на Android/iOS — сделано 2026-08-23** (полное описание — раздел «SEO, фавикон/иконки, PWA-установка» выше). Кратко: canonical/OG/Twitter/JSON-LD/sitemap/robots.txt готовы; своя og-картинка и своя страница `/order/`; фавикон заменён на брендовый (финал — AI-дизайн пользователя, исходник `Site/favicon.png`); сайт устанавливается как приложение на Android (manifest+service worker+кнопка «Установить» в баннере) и на iOS (инструкция «Поделиться → На экран Домой» в том же баннере, программной установки там не существует).
- **Большая мобильная сессия 2026-08-24 — ЗАПУШЕНО В main, ЖИВЁТ НА `www.zilma.pro`.** Пользователь тестировал вживую со своего телефона (через свой Amnezia-туннель на LAN-адрес dev-сервера) весь заход, поймал несколько реальных багов, которых не видно на десктопе:
  - **Полоса «условия доставки» в виджете заявки (`OrderForm.astro`)**, симметричная полосе с мессенджерами в `PriceBlock.astro` — текст: «Доставка по Москве бесплатно — на следующий рабочий день (пн–пт) после подтверждения заказа. В регионы — СДЭК или ваша ТК. Минимальный заказ 5 000 ₽.». Обеим полосам (`.contacts`/`.order-note`) задан одинаковый `min-height:90px` — иначе дивайдеры не совпадали по высоте при разной длине текста (**урок: сверять симметрию координатами `getBoundingClientRect()`, не на глаз**).
  - Мобильный баг в `ArticleCard.astro`: `.card.lg .image-panel` наследовал `flex:0 0 45%` из десктопной строчной раскладки — в мобильной колонке это читалось как доля ВЫСОТЫ, картинка раздувалась, `overflow:hidden` обрезал дату/тег снизу карточки. Фикс — `flex:none` в мобильном медиа-запросе.
  - **`MobileNavBar.astro` — новая плавающая нижняя плашка** (Главная/Каталог/Акции/Инфо), видна только ≤640px, подключена в `BaseLayout.astro`. Каталог/Акции всегда ведут на `/#open-catalog`/`/#open-promo` — единственная страница с обеими модалками; скрипт-обработчик хэша живёт в `PriceChecklist.astro` (общий для всех страниц, где он подключён). Пастельно-голубой полупрозрачный фон с блюром, красные круглые иконки (44px, стиль как у `.contacts a` в прайс-блоке), плавающая карточка со скруглением, авто-скрытие при скролле списка внутри окна (событиями `zilma:nav-hide`/`zilma:nav-show`).
  - **Диалоги Каталог/Акции на мобильном открываются через `.show()`, а не `.showModal()`** — нативный modal-режим делает весь остальной документ `inert`, из-за чего плашка не реагировала на тапы, даже физически её не перекрывая. Диалог укорочен (`calc(100vh - 90px)`), оставляя зазор под плашку.
  - **Переключение Каталог↔Акции с плашки не теряет выбор**: `commitSelection()` переносит отмеченные-но-не-отправленные позиции в `items_json` формы перед закрытием диалога при свитче — проверено многократными переключениями туда-обратно, суммируется корректно (и на телефоне, и на десктопе).
  - **Скрытый баг с `.show()`-диалогами**: `close()` снимает блокировку скролла страницы НЕ синхронно, а с задержкой — обычный `scrollIntoView` сразу после закрытия бился об ещё зафиксированный `<body>` (кнопка «Инфо» на плашке выглядела «неживой», и переход к форме после «Добавить в заявку» на телефоне тоже мог не сработать). Фикс — `closeDialogAndScrollTo()`: сбрасывает блокировку сама, глушит отложенный `unlockPageScroll` флагом, держит целевую позицию несколько кадров.
  - Кнопка «Добавить в заявку» в окнах Каталог/Акции пряталась под плашкой на телефоне — на ≤640px перенесена (`position:absolute`) в шапку рядом с крестиком, цвет по статусу (пастельно-красный неактивна → насыщенный красный активна). Десктоп не тронут (кнопка внизу, как раньше).
  - Кнопка «Отправить заявку» в форме заявки перекрашена в синие тона: пастельно-синяя неактивна → насыщенно-синяя активна (тот же приём).
  - **Service worker больше не регистрируется на деве/локальной сети** (localhost/LAN-адреса) — раньше мог закешировать старую версию скрипта на телефоне между правками, создавая ложное впечатление незакрытых багов; на проде поведение не изменилось.
  - **Новое правило (см. «Как работаем» выше): не закрывать браузер/дев-сервер сразу после проверки — дать пользователю проверить самому.**

- **Виджет заявки как полноценная корзина — ЗАПУШЕНО 2026-08-27.** Повод: пользователь заметил, что клиенты «спотыкаются» при оформлении — после выбора товаров в каталоге видели только строку «Выбрано товаров: N, на сумму X», без перечня и без возможности поправить/убрать позицию. `OrderForm.astro` теперь рендерит корзину построчно (`renderCart()`, реагирует на событие `zilma:cart-updated`):
  - Строка = название (без хвостового артикула — `stripArticle()` отрезает регэкспом `\s+\d{3,8}(?:\/\d{3,8})?\.?$`, т.к. слипание артикула с ценой на узком экране превращалось в нечитаемую цифровую кашу) + цена за штуку + квадратик количества (+/-/ручной ввод) + красный крестик удаления — все 4 элемента визуально одного размера (24×24, `border-radius:6px`), в один ряд с ценой, порядок после цены: количество → ✕ → + → − (по прямому запросу пользователя, с правками по месту через несколько раундов скриншотов с телефона). Артикул убирается только из ОТОБРАЖЕНИЯ — в `items_json`/самой заявке (то, что уходит в Telegram-уведомление) остаётся как есть, для сверки по 1С.
  - **Найден и исправлен реальный баг, объяснивший «квадратики не выровнены»**: CSS для строк корзины вообще не применялся — Astro скоупит `<style>` только к элементам, присутствующим в разметке компонента (добавляет `data-astro-cid-*`), а строки корзины создаются в `<script>` через `document.createElement`, этого атрибута у них нет. Из-за этого кнопки рендерились голым браузерным дефолтом (толстая рамка, разный размер по контенту) независимо от написанного CSS. Фикс — стили строк корзины (`.cart-item*`) вынесены в отдельный **`<style is:global>`** блок в конце компонента. **Урок: любой CSS, нацеленный на элементы, создаваемые в script через `document.createElement` внутри Astro-компонента, должен быть в `is:global` блоке — обычный scoped `<style>` для них не сработает, и без явной проверки computed-стилей в браузере это легко не заметить (визуально «вроде похоже», но не то).**
  - **`MobileNavBar.astro` — добавлен пункт «Корзина»** (между «Акции» и «Инфо» по месту пользователя), ведёт на `/#order`, иконка-тележка, красный бейдж-счётчик поверх иконки (кол-во позиций, скрыт при пустой корзине). Рефакторинг для консистентности: `writeCartItems()` в `OrderForm.astro` стала единой точкой правды — сама шлёт `zilma:cart-updated` при любом изменении (было: событие слал только `PriceChecklist.astro` при добавлении, а изменение количества/удаление внутри самой корзины никого не уведомляло — бейдж бы не узнал). Проверено через `getBoundingClientRect()`/`getComputedStyle()` в браузере (не на глаз): все 4 квадрата ровно 24×24, ряд цена+количество на одной вертикали по центру, 5 иконок плашки помещаются на 390px без переполнения, крестик реально красный (`rgb(200,0,15)`), клик по «Корзина» долистывает точно до `#order`.
- ~~(2026-08-23) Усилить блок «Акции»: подкатегории, обязательно зачёркнутая старая цена рядом с новой~~ — **сделано 2026-08-27** (пункт 12 из «27 идей», см. ниже). Подкатегории — по ступени скидки (по выбору пользователя, а не по бренду/линейке, как в обычном каталоге): «Скидка 15%» / «Скидка 7%», бренд — вложенным уровнем внутри. Заголовки ступеней визуально усилены цветом (по прямой просьбе пользователя со скриншотом): 15% — фирменный красный (`var(--color-red)`), 7% — фирменный синий (`var(--color-blue-dark)`), включая счётчик и стрелку-треугольник (`.pc-tier-15`/`.pc-tier-7` в `PriceChecklist.astro`, новый `groupBy` prop, `'brand' | 'discount'`). У каждого акционного товара — зачёркнутая старая цена рядом с новой (`.pc-old-price`), причём не только в окне «Акции», а везде, где встречаются promo-товары (в т.ч. в обычном «Каталоге»).
  - **Техническая находка: `build_price_current.py` терял исходную цену** — акционная скидка считалась и сразу перезаписывала цену прямо в той же ячейке Excel, старое значение было негде взять на этапе сборки `priceItems.json` (два независимых скрипта, второй читает только финальный xlsx). Решение — сайдкар `Price/promo-meta.json` (та же папка `Price/`, что и остальные рабочие файлы прайса, в `.gitignore` — не в репозитории и не на сайте): `build_price_current.py` пишет туда `{имя_товара: {old_price, discount}}` для каждой акционной позиции, `xlsx-to-price-items.py` подхватывает по имени (та же нормализация пробелов, что и у самого имени) и добавляет `oldPrice`/`discountPct` в JSON только акционным товарам. **Правило на будущее: если снова понадобится тянуть значение, которое `build_price_current.py` перезаписывает/теряет при формировании `price-current.xlsx` — тот же приём (сайдкар в `Price/`), не трогать сам скачиваемый Excel ради внутренних нужд сайта.**
  - **Перед публикацией пересобранного `priceItems.json` источники сверены программно** (не только на глаз): пересобрал тестовую копию `price-current.xlsx` из тех же исходников, что дали живой файл (нашлось не с первой попытки — среди файлов в `Price/` было три похожих кандидата на "прайс от 26 августа", разошлись по количеству совпавших себестоимостей: 1130/1145 у неверной пары, 1142/1148 у верной), сравнил все ячейки — 0 расхождений, кроме 2 уже известных вручную пропатченных строк (см. запись про баг с ошибочной ценой в источнике 1С выше). Только после этого прогнал `xlsx-to-price-items.py` на реальных данных.
- ~~(2026-08-23) «Новинки» — что добавилось в прайс с последнего обновления~~ — **сделано 2026-08-27** (пункт 13 из «27 идей»). Итоговый UI — не отдельная кнопка/окно (сделали и в тот же день убрали по фидбэку пользователя: «лишняя»), а синий бейдж «Нов» прямо у товара — в Каталоге, Акциях, везде, где товар встречается (`.pc-badge-new` в `PriceChecklist.astro`, тот же приём, что и у бейджа «Акция», рядом с ним же). Товар считается «новинкой» 14 дней с момента первого появления в прайсе.
  - **Механизм — накапливаемый журнал, не диф файлов.** `src/data/price-first-seen.json` (**в репозитории**, НЕ в `Price/` и НЕ в `.gitignore` — должен пережить любую пересборку и смену машины/сессии) хранит `{имя_товара: дата первого появления}`. `xlsx-to-price-items.py` при каждом запуске: имя уже в журнале → дата не трогается; имени ещё не было → пишется сегодняшняя дата. `isNew: true` ставится, если дата в журнале не старше 14 дней (`NEW_WINDOW_DAYS`). **Важно про бутстрап**: при самом первом запуске (файла журнала ещё нет) весь текущий прайс задним числом получает дату `сегодня − 15 дней`, а не сегодняшнюю — иначе весь каталог целиком ложно стал бы "новинками" в момент включения фичи. На первом реальном запуске (2026-08-27) новинок закономерно вышло 0 — сравнивать было не с чем, использовались те же исходники 26 августа, что уже были на сайте.
  - **Ретроактивный бэкофилл 2026-08-27, по просьбе пользователя**: сравнили журнал с более старым файлом `Price/общий_прайс_красным_акция_17,08_2026.xlsx` (17 августа) — 118 позиций, которых там не было по точному нормализованному имени. **Перед тем как доверять диффу, проверил на выборке через `difflib.get_close_matches`** — каждое "похожее" совпадение оказалось реально другим товаром (другой объём, другой номер оттенка), не артефактом формата/пробелов. Датам этих 118 позиций в журнале вручную выставлено `2026-08-26` (дата исходника), после чего `xlsx-to-price-items.py` перезапущен для пересчёта `isNew`. **Это разовая ручная операция, не часть обычного пайплайна** — в дальнейшем журнал обновляется сам при каждом обычном прогоне, сравнивать со старыми файлами прайса вручную не требуется.
  - **Правило по просьбе пользователя, 2026-08-27: файлы в `Price/` больше не удаляются без явной просьбы** — держим как историю/бэкап (пригодилось для бэкофилла выше), хотя сам механизм новинок технически от них не зависит (журнал самодостаточен).
- **Интерактивная палитра оттенков — ПИЛОТ на Illumina, ЗАПУШЕНО 2026-08-27** (пункт 10 из «27 идей», см. ниже). Пользователь попросил идею → предложил начать с одной линейки, проверить, насколько надёжно матчится с прайсом, прежде чем тиражировать — согласился, выбрал Illumina («там меньше номеров-оттенков»).
  - **Извлечение цвета — программно, не вручную.** Официальная картинка палитры (`public/images/articles/2026-08-13-illumina-color/palette.webp`, уже была из прошлой сессии) размечена как обычная сетка швотчей на белом фоне — цвета сняты автоматически: connected-component поиск не-белых блобов (numpy, свой BFS, `scipy` не ставили — вне `D:\Projects` пакеты не устанавливаем глобально) с фильтром по размеру/квадратности, цвет каждого блока — медиана по 4 угловым 8×8-блокам (чтобы не зацепить белый текст-код по центру свотча). Из 49 официальных оттенков (18 cool + 20 warm + 7 neutral + 4 Opal-Essence — сумма сошлась с текстом статьи) блоб-детектор нашёл 38 сразу; 7 neutral не разделились (слиплись в одну колонку без зазора) и 2 opal слились соседними — досчитаны точечным сэмплингом по интерполированным координатам (шаг колонок/строк регулярный, вычислен из уже найденных). Результат сверен визуально реконструкцией собственной мини-картинки-грида рядом с оригиналом — совпадает.
  - **Связка с прайсом — по регэкспу на имя, сработало на удивление чисто.** Для Illumina 1С-имя товара имеет вид `Краска {код} {название} 60мл {код}` (код продублирован) — `^Краска\s+(\S+)\s` даёт код напрямую, без эвристик по article(). Для Opal-Essence (не числовые коды, названия) — сматчено по русским названиям из прайса (`Лиловое Серебро`→Silver Mauve, `Платиновая лилия`→Platinum Lily, `Титановый Розовый`→Titanium Rose) вручную, «Copper Peach» в прайсе не нашёлся вообще. Итог: 37 из 49 оттенков сматчились автоматически, остальные 12 показываются в сетке притемнёнными с пометкой «нет в актуальном прайсе» — честно, не выдумываем цену. Данные — `src/data/illumina-shades.json` (code/hex/group/row/col + name/price если сматчилось), компонент — `src/components/ShadeSwatchGrid.astro`, подключён в `src/pages/articles/[id].astro` только для `id === '2026-08-13-illumina-color'` (не общий для всех статей, пилот).
  - **Побочный, более важный результат: корзина переведена на localStorage.** Пилот сразу же обнажил реальный пробел — «Добавить в заявку» с страницы статьи технически не мог работать, потому что вся корзина держалась в одном скрытом поле формы, которое физически существует только на главной странице (`OrderForm.astro`, только `index.astro`). Решение — общее хранилище `window.zilmaCart` (`src/components/CartStore.astro`, `localStorage`, ключ `zilma-cart`), подключено в `BaseLayout.astro` **инлайн-скриптом** (`is:inline`, не модуль — гарантированно выполняется синхронно раньше остальных скриптов страницы, без риска гонки с порядком module-скриптов) перед `<slot />`, то есть на КАЖДОЙ странице сайта. `OrderForm.astro` (скрытое поле теперь просто зеркало для отправки формы, синхронизируется из хранилища при каждом `zilma:cart-updated` и при загрузке страницы), `PriceChecklist.astro` (`commitSelection()`) и бейдж-счётчик в `MobileNavBar.astro` переключены с прямого чтения/записи `input[name="items_json"]` на `window.zilmaCart.get()/.set()`. Работает в обе стороны: товар, добавленный со статьи, виден в корзине на главной сразу при загрузке (без обновления вручную), а бейдж на плашке теперь верно считает на ЛЮБОЙ странице сайта, не только на главной — раньше он тихо был сломан везде, кроме `index.astro`, просто это было незаметно, пока корзину нельзя было наполнить не с главной. **Компромисс, явно проговорённый с пользователем**: корзина per-device — не синхронизируется между телефоном и компьютером, живёт до очистки данных сайта в браузере; полноценный сервер/аккаунт не делали, для масштаба сайта не нужен.
  - **Ещё не сделано, если решим тиражировать на другие линейки**: тот же пайплайн извлечения цвета + матчинга нужно будет прогонять по каждой линейке отдельно (код заточен под структуру Illumina-конкретной сетки cool/neutral/warm + Opal — у других линеек могут быть другие форматы кодов в 1С-имени, не факт что регэксп `^Краска\s+(\S+)\s` сработает так же чисто).

**Открытые задачи на следующую сессию:**
1. ~~Проверить, не снят ли Color Touch Relights с производства~~ — **сделано 2026-08-17, подтвердилось**: на официальном wella.com (`/professional/en-EN/hair-color/color-touch`) в текущей линейке только Color Touch и Color Touch Plus — Relights, Sunlights, Instamatic не упоминаются вовсе, значит сняты с производства. Разделы по ним удалены из `2026-08-13-color-touch.md` (картинки `variant-relights/sunlights/instamatic.png` на диске оставлены нетронутыми, просто больше не используются). **На официальном сайте также нашлась новая услуга Color Touch Melt, которой нет в `WELLA_COLOR_TOUCH_FINAL.pdf`** — не добавлена, ждёт решения пользователя, добавлять её или нет.
2. ~~Переделать статью Koleston Perfect~~ — **сделано 2026-08-17**: гид-виджеты применены ко всем разделам, обложка заменена на чистое фото товара.
3. ~~Переделать Color Touch~~ — **сделано 2026-08-17**: контент полностью пересобран из `WELLA_COLOR_TOUCH_FINAL.pdf` (рендер страниц + визуальная сверка чекбоксов «область применения» по каждой линии, не только текстовый слой), старый контент из другого источника (Crystal Gloss микс 1:1:1, Grey Blending и т.п. — их не было в этом PDF) удалён. Виджеты применены, включая новый `.checklist`.
4. **Перенести дизайн-систему гид-виджетов на оставшиеся 9 статей** (Londa, Londa Color Tune, Igora Royal, Matrix SoColor/Color Sync/Tonal Control, Lebel MATERIA/MATERIA G/MATERIA μ) по готовому списку классов. **Источники PDF неровные — сверять перед началом**: Igora Royal — есть полный `igora-royal-technical-manual.pdf` (рус.); Shinefinity был готов из `Shinefinity_StarterGuide на англ.pdf`; для Londa (базовая линия) и Lebel MATERIA μ официального PDF в `Работа/` не нашлось — нужен файл от пользователя; Matrix (SoColor/Sync/Tonal Control) — есть только карты прядей, без пропорций/времени, тоже нужен более полный источник или консультация с пользователем. — стиль обложки и виджетов уже не открытый вопрос, просто применять. **Для каждой линии сначала визуально сверять содержимое с официальным PDF-источником этой линии** (текст + рендер страниц, включая чекбоксы/детали, которых нет в текстовом слое) — не доверять слепо старому контенту статьи, если он был.
5. ~~Платформа для деплоя подтверждена пользователем 2026-08-17: Vercel~~ — **сделано (2026-08-18/22)**: задеплоено на Vercel, живой домен `www.zilma.pro` + `zilma-site.vercel.app`. Полные детали — в разделе «Деплой — сделано» выше, не здесь.
6. **Аудит 4 готовых статей завершён 2026-08-18** (запущено 8 субагентов параллельно: `site-content-auditor` + `site-image-auditor` × 4 статьи). Полные тексты отчётов агентов — в истории сессии 2026-08-18, при необходимости поднять оттуда детали/источники. **Текстовые находки внесены во всех 4 статьях к 2026-08-22** (см. отметки ✅ ниже) — осталось только по картинкам (палитры/упаковка), это отдельная задача, ещё не начата.

   **Koleston Perfect — ЗАКРЫТО ПОЧТИ ПОЛНОСТЬЮ 2026-08-23:**
   - ✅ Фактическая ошибка исправлена: «1 мл Wellaplex №1 Bond Maker на 30 г краски» → **2 мл**.
   - ✅ Добавлены раздел «Область применения» (`.checklist`) и блок про технологию ME+.
   - ✅ **«13 недостающих оттенков» из отчёта аудита 2026-08-18 не подтвердилось при повторной программной проверке** — скачан тот же официальный PDF, текстовый слой страницы с палитрой распарсен регуляркой и сверен построчно с кодами на старой картинке: разница ровно **1 оттенок** (`7/41`, Vibrant Reds), не 13. Похоже, число «13» в исходном отчёте субагента было ошибкой/галлюцинацией — **урок: числовые находки такого рода агентов стоит перепроверять программным сравнением списков, а не доверять на слово.** `palette.webp` пересобрана из того же официального PDF (тот же источник, что уже использован для Color Touch/Illumina/Shinefinity в этой сессии).
   - ⚠️ Ещё не перепроверено (не блокирует, второстепенное): время выдержки «15-20 vs 15-25 мин с теплом» (Pure Naturals/Vibrant Reds/Deep Browns) и «5-10 vs 5-15 мин» (Pastel Toning) — источники расходятся между собой, нужен самый свежий factsheet как арбитр. Мусорный битый `allergy-step4.png` на диске не используется — можно удалить, вреда нет.
   - ✅ **Флакон-оксид на фото товара обновлён (найдено пользователем, не аудитом, 2026-08-23).** Старое фото `hero-product.webp`/`cover.webp` показывало устаревший дизайн флакона Welloxon Perfect (белый флакон с непропорционально высоким горлышком — похоже, артефакт исходной сборки из PDF). Официальный магазин WellaStore подтвердил актуальный дизайн (компактное горлышко, обновлённая графика на этикетке). `hero-product.webp` пересобран как флакон+коробка из свежих чистых фото (тот же приём, что для Color Touch). Для `cover.webp` пробовал вклеить только новый флакон в старую сцену с 6 предметами — тень/опора не совпадали, выглядело криво; вместо этого упростил обложку до пары флакон+коробка, как у Illumina/Shinefinity. **Урок: аудит проверял только текст и палитры — фото упаковки/оборудования на предмет актуальности дизайна не проверялись вовсе (кроме единственного явного случая Color Touch, который уже был найден агентом) — при следующей ревизии статей стоит явно сверять КАЖДОЕ товарное фото с официальным источником, не только то, на которое агент указал.**

   **Color Touch — ЗАКРЫТО ПОЛНОСТЬЮ 2026-08-23 (текст + картинки):**
   - ✅ «24 раза мытья шампунем» → **28**.
   - ✅ Light2Color/Tri-Spectra заменено на **Metal Purifier**.
   - ✅ Добавлен раздел **Color Touch Melt**.
   - ✅ Факт о веганской/безсиликоновой формуле и цифра «90+ оттенков» — уже были в тексте (найдено при проверке, чек-лист ошибочно считал их не внесёнными).
   - ✅ **Упаковка пересобрана** (`cover`, `hero-product`, `variant-plus`) под актуальный дизайн Wella (сплошной градиент вместо вихревого узора) — источник: официальные фото товара с `uk.wella.professionalstore.com` (флакон-эмульсия + туба+коробка сфотографированы по отдельности, `hero-product`/`cover` собраны композицией через PIL). `variant-plus` теперь показывает именно линейку Plus, не базовую.
   - ✅ `palette.webp` пересобрана из актуального официального PDF (`wella.com/professional/m/Shadechart_LP/Wella-Professionals_Color-Touch_Leaflet.pdf`, та же схема URL что у Koleston) — теперь включает семью Color Touch Plus, которой не было на старой картинке.

   **Illumina Color — ЗАКРЫТО ПОЛНОСТЬЮ 2026-08-23 (текст + картинки):**
   - ✅ «47 оттенков» → **49**, добавлены 4 новых Warm Shimmer оттенка (9/37, 8/36, 7/42, 7/75) — в тексте и в `description`.
   - ✅ Добавлено время выдержки для базовой линии (30-40 мин без тепла / 15-25 мин с теплом при окрашивании; 20/10 и 30-40/15-25 при осветлении).
   - ✅ Добавлено упоминание технологии Metal Purifier в паре с MICROLIGHT.
   - ✅ **Color.id разрешено**: скачан текущий официальный лифлет (`wella.com/professional/m/Shadechart_LP/Wella-Professionals_Illumina_Leaflet.pdf`) — Color.id там не упоминается вообще, вместо него официальная схема закрашивания 70-100% седины (1:1 нейтральный оттенок + Welloxon 6%). Таблица с Color.id заменена на эту схему.
   - ✅ **Chrome Olive разрешено**: в том же официальном PDF секция Opal-Essence содержит ровно 4 оттенка (Silver Mauve, Platinum Lily, Titanium Rose, Copper Peach) — Chrome Olive среди них нет. Убран из статьи, «пять оттенков» → «четыре» везде (текст, подпись картинки, палитра).
   - ✅ `palette.webp` пересобрана из этого же официального PDF — состав точно совпадает с текстом статьи (49 оттенков, 4 Warm Shimmer, 4 Opal-Essence).

   **Shinefinity — ЗАКРЫТО ПОЛНОСТЬЮ 2026-08-23 (текст + картинки):**
   - ✅ Убрана неподтверждённая деталь «(без кондиционера)».
   - ✅ Исправлен перевод «towel-dried» → «подсушенные полотенцем» для Glaze и Flash Glaze (было ошибочно «влажные после мытья»).
   - ✅ `palette.webp` пересобрана из официального лифлета (`wella.com/professional/m/Shadechart_LP/Wella-Professionals_Shinefinity_Leaflet.pdf`) — добавлены 5 недостающих кодов (04/0, 06/0, 08/0, 04/12, 07/12). Старая картинка вдобавок вводила в заблуждение: её ряд «010/xx» на деле не отдельные товары, а результат смешивания 09/xx + бустер 00/00 (было подписано мелким текстом на самой картинке) — в новой картинке оставлены только 3 настоящих 010-оттенка (010/00, 010/6, 010/8). 4-й бустер (00/56) в этом официальном источнике не нашёлся — не добавлен, не выдумывать.
   - Остальное (пропорции, форматы услуги, pH-механика, бустеры) подтверждено дословно, без изменений.

   **Аудит 4 готовых статей закрыт полностью 2026-08-23** — и текст, и картинки (палитры у всех 4, упаковка Color Touch) обновлены и закоммичены. Единственное, что осталось необязательным «на подумать» — перепроверка времени выдержки у Koleston (см. пункт выше, второстепенное).
- ~~Мобильная адаптивность прописана в CSS (медиа-запросы 640/720/900px), но не проверена скриншотом~~ — **частично проверено и найден реальный баг 2026-08-24.** `mcp__claude-in-chrome__resize_window` в этой среде по-прежнему не меняет реальный viewport (репортит успех, но `window.innerWidth` не меняется) — рабочий обходной путь: **`mcp__playwright__browser_resize`** (отдельный движок Playwright, не расширение в реальном Chrome пользователя) реально меняет viewport и корректно триггерит CSS-медиа-запросы — использовать его для любых проверок мобильной вёрстки в следующих сессиях, `resize_window` для этого больше не пробовать.
  - **Найден и исправлен баг (2026-08-24):** на крупной карточке статьи (`.card.lg`, лента статей на главной) в мобильной раскладке (≤720px) дата/тег/стрелка были обрезаны `overflow:hidden` карточки — обрубает у Shinefinity. Причина: `.card.lg .image-panel{flex:0 0 45%}` (доля ШИРИНЫ в десктопной строчной раскладке) не был сброшен в мобильном медиа-запросе, где `.card.lg` становится колонкой — те же `45%` там читаются как доля ВЫСОТЫ, картинка раздувается, карточке не хватает высоты на текстовый блок. Фикс — `flex: none` добавлен в `.card.lg .image-panel` внутри `@media(max-width:720px)`, `ArticleCard.astro`. **Урок: при переключении flex-direction row→column в медиа-запросе — всегда явно проверять/сбрасывать flex-basis/proportions у детей, они не сбрасываются сами.**
  - Остальные брейкпоинты (640/900px, прайс/заявка/фильтры) визуально не проверены тем же методом — можно перепроверить по накатанной схеме (playwright resize + `getBoundingClientRect` на границах карточек), пока не проверялось за пределами ленты статей.
7. **Баннер установки (`InstallPrompt.astro`) — визуально проверен только на десктопном Chrome вручную (принудительно, через JS-консоль)**, не на реальном мобильном экране — `resize_window` не сработал в этой сессии (тот же инструмент, что и не сработал для мобильной адаптивности выше). CSS построен на относительных величинах, должен адаптироваться сам, но глазами на реальном телефоне ещё не подтверждено — пользователь тестирует сам. **Установка на Android и подсказка на iOS (Safari/Chrome) сделаны и задеплоены**, но пользователю ещё предстоит реально удалить/переустановить старые ярлыки на своих устройствах, чтобы увидеть новую иконку/сплэш — сама по себе она не обновится (ограничение ОС, не починка).

# План развития сайта — растущий список
Это НЕ то же самое, что «Открытые задачи» выше (та секция — снимок конкретной сессии, смешаны выполненные и открытые пункты). Здесь — постоянно пополняемый список на будущее: сюда попадают мои рекомендации по улучшению сайта, KPI из аналитики (когда пользователь начнёт делиться данными Vercel Analytics или другой метрикой) и идеи самого пользователя. Заведено 2026-08-23 по прямой просьбе («запиши как план на будущее... будем добавлять kpi из аналитики и мои идеи»).

**Формат:** дата добавления + пункт + при необходимости оценка сложности. При выполнении — не удалять, а зачёркивать (`~~текст~~`) с пометкой «сделано (дата)», как везде в этом файле — история решений тоже ценна. **Каждое изменение этого раздела дублировать в `progress-log.html`** (раздел «Что дальше», см. правило в «Как работаем» выше про параллельное ведение обоих файлов) — переписав по-человечески, не копией.

## Фронтенд
- (2026-08-23) Своя страница 404 вместо голой служебной заглушки Vercel. Сложность: низкая.
- ~~(2026-08-23) Прогнать Lighthouse-аудит~~ — **сделано 2026-08-23** (через pagespeed.web.dev, реальные данные, не Lighthouse из DevTools, но то же самое ядро): мобильная версия главной — **Производительность 74, Доступность 95, Рекомендации 100, SEO 100** из 100. SEO/Рекомендации — максимум, уже хорошо. Производительность 74 — не критично, но есть конкретная находка ниже.
- ~~(2026-08-23) Найдена реальная причина просадки производительности: `global.css` подключал шрифты через `@import url(fonts.googleapis.com...)`~~ — **сделано 2026-08-26**: заменено на `<link rel="preconnect">` + `<link rel="stylesheet">` в `<head>` (`BaseLayout.astro`), `@import` убран из `global.css`. Общий layout — эффект сразу на всех страницах (главная, все статьи, `/order/`), отдельно на статьях ничего дополнительно настраивать не нужно.
- ✅ **Яндекс.Вебмастер подключён — сделано 2026-08-26** (Google Search Console пока нет, см. ниже). Полная цепочка: (1) пользователь добавил сайт на webmaster.yandex.ru, выбрал верификацию мета-тегом, прислал код — добавлен `<meta name="yandex-verification" content="94926554404b9c62" />` в `BaseLayout.astro`, задеплоено, права подтверждены (роль «Владелец»); (2) sitemap `https://www.zilma.pro/sitemap-index.xml` добавлен и встал в очередь на обработку; (3) регион «Москва» отправлен через «Представление в поиске → Региональность» (не там, где ожидалось — не в «Индексирование»/«Инструменты»/«Оптимизация сайта», нашли только через веб-поиск), со ссылкой-подтверждением на главную страницу сайта (там есть текст «Доставка по Москве бесплатно...») — заявка на модерации, до 7 дней. Выполнено через `mcp__claude-in-chrome__*` в реальной залогиненной сессии пользователя (он сам зашёл и сказал «можешь сам пользоваться»), не текстовыми инструкциями — почти сразу быстрее и без десятка раундов «покажи скриншот».
- (2026-08-23) **Google Search Console не подключена** — GSC, в отличие от Яндекса, для этого проекта менее приоритетна (см. пункт выше про перекос трафика не в пользу России), но всё ещё стоит сделать: бесплатно, нужен логин пользователя в Google, аналогичный мета-тег `google-site-verification` в `BaseLayout.astro`.
- (2026-08-23, перепроверено 2026-08-26 — статус не изменился) **Подтверждено реальным поиском: сайт сейчас не находится по своему имени.** Запросы `zilma.pro` и «Zilma профессиональная косметика для салонов» не показывают сайт вообще — см. разбор стороннего отчёта выше. Прямое следствие пункта про Search Console/Вебмастер + того, что сайт нигде не продвигался и на него нет внешних ссылок — ожидаемо для нового домена, но раньше это было предположением, теперь — проверенный факт. **Не отдельная задача — уже в работе**: 2026-08-26 подключён Яндекс.Вебмастер (права, sitemap, регион «Москва» на модерации) именно ради этого; эффект не мгновенный, Яндекс сам оценивает индексацию в 1-2 недели. Google Search Console по-прежнему не подключена — второй нужный шаг, ещё не сделан.
- (2026-08-23) **Форма заявки (`/api/order`) не защищена от спама** — нет ни rate-limit, ни капчи, ни honeypot-поля. Сейчас любой скрипт/бот может завалить Telegram сотнями фейковых заявок. Сложность: низкая-средняя (простой honeypot + серверный rate-limit по IP решают большую часть).
- (2026-08-23) Файл-вложение в форме заявки проверяется на сервере только по размеру, не по типу — ограничение `accept=".pdf,.doc..."` на файле чисто клиентское, легко обойти, в Telegram может прилететь файл любого формата. Риск невысокий (файл просто пересылается в Telegram, не выполняется на сервере), но стоит поправить. Сложность: низкая.
- ~~(2026-08-23) Vercel Speed Insights (отдельный от Analytics бесплатный инструмент — реальные данные о скорости загрузки у настоящих посетителей, Core Web Vitals) — не подключён~~ — **сделано 2026-08-27**: `npm install @vercel/speed-insights`, компонент `<SpeedInsights />` (`@vercel/speed-insights/astro`) добавлен в `BaseLayout.astro` рядом с уже существующим `<Analytics />` — тот же приём, ничего больше не менялось. Данные о Core Web Vitals появятся в панели Vercel по мере реальных заходов, не сразу.
- (2026-08-23) **Каталог заявки (`PriceChecklist.astro`) рендерит все 1154 позиции сразу в DOM**, просто скрывая незаинтересовавшие бренды/линейки через `<details>` — при такой реальной величине (проверено: 17 брендов, 1154 товара в `priceItems.json`) виртуализация списка (рендерить только видимые строки) — не преждевременная оптимизация, а разумная мера против подтормаживания на слабых телефонах. Источник идеи — сторонний отчёт (см. «Урок» ниже), но объём подтверждён напрямую по данным. Сложность: средняя.

## Контент
- (2026-08-17) Перенести дизайн-систему гид-виджетов на оставшиеся 9 статей (Londa, Igora Royal, Matrix, Lebel MATERIA) — полный список источников и что по каким брендам не хватает, см. «Открытые задачи» выше, пункт 4. Ждёт PDF-источников от пользователя по части линеек.
- (2026-08-23) **Прайс устарел на 6 дней** — `src/data/prices.ts` показывает дату `2026-08-17`, сегодня `2026-08-23`. Не техническая проблема, а операционная: проверить, актуальны ли ещё цены/наличие, обновить файл+дату если нужно. Дополнение 2026-08-23: можно не только проверять руками каждый раз, а завести себе напоминание (календарь/чат-бот) обновлять прайс на регулярной основе — обсудить формат с пользователем.
- (2026-08-23) На главной нет отдельного блока «Почему мы»/преимущества (профессиональные бренды, стабильность поставок, поддержка и т.п.) — сейчас сразу после хиро идут прайс-блоки, доверие приходится строить только брендами в подзаголовке. Единственная реально новая, применимая идея из стороннего отчёта ChatGPT (остальное там либо уже сделано, либо не подходит для сайта-визитки — см. разбор ниже, перед разделом KPI). Сложность: низкая, чисто контентный блок.
- (2026-08-23) **В хиро на главной показаны только 5 брендов (Wella · Londa · Schwarzkopf · Matrix · Lebel), хотя в реальном каталоге (`priceItems.json`) их 17**: CAREPROST, CONCEPT, DIKSON, ESTEL, ISKARTES PROFESSIONAL, KAPOUS, L'OREAL, LEBEL, LEVISSIME, LONDA, MATRIX, OLLIN, SCHWARZKOPF, TIGI, WELLA, плюс «Корея» и «Одноразовая продукция» (это, похоже, категории/сборные группы, не бренды в привычном смысле — уточнить у пользователя). Стоит решить: расширять список в хиро/на отдельный блок брендов, или осознанно оставлять только 5 «флагманских» (сейчас, кажется, просто так сложилось, а не осознанное решение показывать только пять). Сложность: низкая, вопрос содержания, не кода.
- (2026-08-23) **Вкладка фильтра «Уход» сейчас пустая** (проверено — все 4 готовые статьи размечены только тегом `окрашивание`, ни одна не `уход`) — реальная «мёртвая вкладка», как и было сказано в стороннем отчёте. Решится само по мере переноса оставшихся 9 статей, если среди них будут уходовые линейки — либо нужно явно спланировать хотя бы 1-2 статьи именно про уход. **Пользователь сразу же предложил конкретные темы (2026-08-23), закрывающие именно этот пробел:**
  - Статья про восстановление волос — линейка «Счастье для волос» (Lebel) и другие наборы этого бренда.
  - Статья про спа-процедуру — ампулы Dikson + маска для повреждённых волос Londa.
  - Источники (официальные PDF/сайты брендов) для этих тем ещё не собраны — уточнить у пользователя при старте, откуда брать материал (аналогично тому, как это делалось для уже готовых 4 статей).
- (2026-08-23) Блок «Условия сотрудничества» (доставка, оплата, минимальный заказ) — сейчас на сайте этого нет вообще, только прайс и форма заявки. Разумное дополнение для B2B-аудитории (мастера/салоны обычно первым делом спрашивают именно это). Сложность: низкая, контентный блок.

## Документация
- ~~(2026-08-23) Обновить README.md~~ — **сделано 2026-08-23**: добавлены разделы «SEO» и «Установка сайта как приложения (PWA)», поправлена ссылка на сайт (`zilma.pro` → канонический `www.zilma.pro`), в структуру проекта добавлена `order.astro`.
- (2026-08-23) Добавить `.env.example` с `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` — сейчас эти переменные описаны только текстом в README, шаблона файла нет.

## Юридическое (не техническое — важно для бизнеса)
- (2026-08-23) **Форма заявки собирает имя+контакт+файл без явного согласия на обработку персональных данных** (чекбокс + ссылка на политику обработки ПДн) — по 152-ФЗ («О персональных данных») это обычно требуется для российских сайтов, которые собирают такие данные через форму. **Я не юрист — это не юридическая консультация, а технический флаг**: стоит уточнить у бухгалтера/юриста пользователя, обязательно ли это в его случае. Если решит, что нужно — могу подготовить типовой чекбокс на форме + отдельную страницу с политикой (сложность низкая-средняя, но текст политики должен утвердить сам пользователь или его юрист, не я).
- (2026-08-23) Как только заведём Яндекс.Метрику (см. раздел про аналитику ниже) — она использует куки для трекинга, тогда же встанет вопрос про уведомление о cookies на сайте. Сейчас у сайта cookie-баннера нет (Vercel Analytics куки не использует, поэтому раньше вопрос не возникал).
- (2026-08-23) В футере сейчас только копирайт-строка — нет юр. реквизитов (ИП/ООО, ИНН и т.п.) и ссылки на политику конфиденциальности. Тот же корень, что и пункт про согласие на обработку персональных данных выше — оба решаются вместе, одной страницей с политикой + её упоминанием в футере и на форме заявки.

**Урок 2026-08-23 (проверять сторонние AI-отчёты, не копировать как факт):** пользователь прислал «аудит сайта» от Алисы (Яндекс) — текст выглядел правдоподобно, но начинался с признания «нет данных об аналитике zilma.pro» и дальше шли общие фразы без единой цифры по этому конкретному сайту. Проверка показала: часть утверждений («дубли Title/Description», «видно в Яндекс.Вебмастере/Search Console») — неправда (проверено напрямую по коду и живым страницам, Search Console/Вебмастер вообще не подключены). Реальную пользу дала не вера отчёту, а собственная проверка через pagespeed.web.dev (настоящие цифры) + grep по коду. **Правило: сторонние «аудиты» от других AI/сервисов — повод перепроверить конкретные пункты самому, не повод копировать их в план как факт.**

**Второй сторонний отчёт (2026-08-23, от ChatGPT) — тот же урок, другой результат.** Этот ассистент сам честно предупредил, что не смог открыть сайт («500 ошибка после редиректа zilma.pro → www.zilma.pro»), и дальше рассуждал по шаблону «типичный проф.косметика сайт», а не по реальному содержимому. Проверка:
- **«500 ошибка» — неправда.** Проверил сам curl'ом (в т.ч. с User-Agent Googlebot и GPTBot) — редирект `zilma.pro` → `www.zilma.pro` везде отдаёт нормальный `200`, ошибки нет. Похоже, споткнулся именно инструмент ассистента, не сайт.
- **«Сайт не находится в поиске» — ПРАВДА, подтверждено реальным поиском.** Запрос `zilma.pro` в поиске не показывает сам сайт вообще — выдаёт не связанные с нами сущности с тем же именем (индийская SaaS/POS-компания Zilma, `zilmahq.com`, Facebook-страница ZILMA, виртуальный питомец Webkinz). Запрос «Zilma профессиональная косметика для салонов» вообще не находит наш сайт — только сайты конкурентов. Это ожидаемо и объяснимо тем, что мы уже знаем (домен новый, Search Console/Вебмастер не подключены, внешних ссылок на сайт нет) — но теперь есть конкретное доказательство последствия, не только теория.
- **Путаница с одноимённым брендом — реальный, но некритичный риск.** Существуют другие «Zilma»/«ZILMA» (индийский SaaS, `zilmahq.com`, Facebook-страница) — при продвижении стоит явно закреплять словосочетание «профессиональная косметика для салонов» рядом с именем бренда, не полагаться на голое «Zilma» в SEO-текстах и рекламе.
- **Остальные рекомендации (реструктура каталога по техникам окрашивания, 5-блочная структура главной, позиционирование «для колористов»)** — построены на предположении, что сайт полноценный e-commerce каталог, которым он не является (это сайт-визитка с прайсом+гидами, не магазин с карточками товара по каждому оттенку). Часть уже реализована по факту (позиционирование «для салонов» уже в хиро, бренды-производители уже чётко подписаны в каждой статье — Wella Koleston Perfect и т.п., не создают впечатление собственных линеек Zilma), часть (контент про колористику — формулы, тонирование, седина) уже и есть план по гид-статьям (см. «Контент» выше). Из нового и применимого — только один пункт, добавлен ниже.

**Третий сторонний список (2026-08-23, без указания источника, но по стилю — тоже AI-сгенерированный: заканчивался фразой от первого лица «если хочешь, могу оформить это в документ...»).** Формат «от простого к сложному» удобный, но так же требовал проверки:
- **«Добавить alt ко всем картинкам» — неправда.** Проверено grep'ом по всем `<img>` в `src/components/`, `src/layouts/`, `src/pages/` — alt есть у каждой. Не добавлено в план.
- Остальное подтвердилось или оказалось разумным — раскидано по своим разделам (Фронтенд/Контент/Юридическое/Идеи) выше и ниже, не свалено одним списком.

## KPI из аналитики
Данные и находки из Vercel Analytics (панель зафиксирована 2026-08-23, окно «последние 7 дней» — выборка маленькая, 49 визитов, рано делать далекоидущие выводы, смотреть в динамике):
- 49 визитов / 204 просмотра / **57% отказов (bounce rate)** — многовато на глаз, но при такой маленькой выборке пока не факт, что это реальная проблема, а не шум. Отслеживать, не паниковать на одной неделе данных.
- Топ-страницы: главная (49), статья Koleston Perfect (7), Shinefinity (4), Color Touch (3), Illumina (2), `/order/` (1).
- Гео: США 35%, Британия 27%, Франция 8%, Молдова 8%, Россия 8% — при том что аудитория сайта задумана российская (салоны красоты РФ), доля РФ-трафика подозрительно маленькая. Возможные причины (не проверено): рано делать выводы при 49 визитах, боты/краулеры в статистике, либо реальный трафик пока не из целевой аудитории (сайт ещё нигде не продвигался).
- Устройства: 57% десктоп / 43% мобильный — доля мобильных выше, чем часто предполагают для B2B, лишний повод не откладывать проверку мобильной адаптивности (см. «Фронтенд» выше).
- **Главный пробел: конверсия не отслеживается вообще.** Vercel Analytics на бесплатном тарифе считает только просмотры страниц — сколько людей реально нажали «Выбрать и заказать» или отправили заявку, сейчас никак не видно. Кастомные события в Vercel Analytics — платная Pro-функция (проверено в панели). Решение — Яндекс.Метрика (бесплатно, с целями и Вебвизором) — пользователь уже согласился завести счётчик 2026-08-23, ждём ID счётчика от пользователя, дальше нужно настроить минимум 3 цели: клик «Выбрать и заказать», клик «Скачать EXCEL», успешная отправка заявки.

## Идеи пользователя
Записаны как есть, своими словами пользователя, без доспецификации — детали уточнять при взятии в работу, не сейчас.

- (2026-08-23) **Решить с формированием сайта.** Пока без деталей — судя по контексту, скорее всего про общее направление/структуру (сайт-визитка как сейчас vs что-то более крупное) на фоне разбора стороннего SEO-отчёта в этой же сессии (см. «Урок... от ChatGPT» выше). Уточнить у пользователя, что конкретно имеется в виду, когда дойдёт очередь.
- (2026-08-23) **В блок «Оформить заявку» добавить ботов WhatsApp, Bip, email (возможно MAX).** Сейчас контакты (иконки-ссылки на мессенджеры) есть только в `PriceBlock.astro`, не в самой форме `OrderForm.astro`. Судя по слову «ботов» (не просто ссылки) — вероятно, речь о том же, что уже есть для Telegram (`/api/order` шлёт заявку ботом в Telegram) — сделать похожую автоматическую доставку заявок через WhatsApp Business API / Bip / email-автоответчик, и, возможно, через MAX (мессенджер, один из рабочих каналов пользователя — см. `work/CLAUDE.md`). Крупная задача, нужно уточнять реализуемость и стоимость по каждому каналу отдельно (WhatsApp Business API обычно платный).
- (2026-08-23) **Добавить на сайт статьи, уже собранные в Telegram-канале (папка NEWS), проверив и обновив актуальность инфо/картинок.** Пересекается с уже существующей задачей «перенести дизайн-систему на оставшиеся 9 статей» (см. «Контент» выше) — возможно, это одно и то же, возможно пользователь имеет в виду более широкий список тем из канала, не только 9 брендовых гидов. Уточнить охват при старте работы.
- (2026-08-23) **Статья/инструмент «конверсия цветов»** — интерактивный виджет («бот в окне») для подбора соответствия номеров оттенков между брендами (например, «какой оттенок Wella соответствует Londa 7/1»). Новая фича, не статья в привычном смысле — нужно продумать, на основе какого источника данных сопоставлять номера между брендами (готовых таблиц соответствия у разных производителей может не быть).
- (2026-08-23) **Калькулятор срока годности** (возможно тоже как бот/виджет) — пользователь вводит дату производства/партию, получает срок годности или сколько осталось. Нужны будут данные о сроках годности по каждому бренду/линии (обычно 24-36 мес. с даты производства для проф. косметики, но нужно подтверждать по каждому бренду отдельно, не считать одинаковым для всех).
- (2026-08-23) **Полноценный каталог с фильтрами (бренд/линейка/тип) как отдельная страница со своими URL на каждый бренд** (не только модалка «Выбрать и заказать») — по сути и есть содержательный ответ на идею пользователя «решить с формированием сайта» выше и на подтверждённую находку «сайт не находится в поиске» (см. «KPI» ниже): страница `/catalog/wella/` реально может получать трафик по запросам вида «Wella прайс», текущая модалка — нет (не индексируется поисковиками). Крупная задача, ждёт решения по направлению сайта.
- (2026-08-23) **Личный кабинет для постоянных клиентов** (история заказов, повтор заявки в 1 клик) — требует базы данных и авторизации, которых сейчас в проекте нет вообще (весь сайт — статические файлы + одна серверная функция для формы). Самая крупная и дорогая из идей, не начинать без явного решения пользователя.
- (2026-08-23) **Автоматизация актуализации прайса** (импорт из 1С/от поставщика вместо ручного обновления Excel) — перекликается с более ранней идеей пользователя про автоматический расчёт наценки 28% для акционного прайса из 2 Excel-файлов (см. память `project_promo-price-generation.md` из прошлых сессий) — возможно, стоит решать оба вопроса вместе, не по отдельности.

# Крупное видение — «ZILMA как рабочая платформа для колориста» (на обдумывание, 2026-08-23)

**Отдельный блок по прямой просьбе пользователя** — не растворять по категориям плана выше, как остальные сторонние отчёты в этой сессии: это стратегическое видение, не набор точечных технических находок, оценивать и обсуждать нужно целиком. Источник — сторонний AI-отчёт (по стилю и структуре — похоже на GPT), в этот раз явно на основе реального осмотра live-сайта `www.zilma.pro` (не выдумано с нуля, как было со вторым отчётом раньше в этой же сессии). Пользователь: часть идей «нужно обдумать», часть «интересные, нужно рассмотреть», возможно дополнит своими.

**Проверил один конкретный факт из отчёта — подтвердился:** в `priceItems.json` действительно есть товар с «не заказывать!» прямо в названии — `ST Soft Care Шампунь бессульфатный д/деликатного очищения 1000мл не заказывать! 91247`. Реальный кейс, не выдумка. Остальное в отчёте — не фактические утверждения, а предложения/мнения, факт-чекать там особо нечего (кроме этого пункта).

**Общая оценка сайта от автора отчёта** (субъективно, не мой вывод, привожу как есть): Функциональность 8/10, Практическая ценность 9/10, Каталог/ассортимент 9/10, Удобство для постоянного клиента 8/10, Визуальная подача 7/10, Навигация 6/10, SEO-потенциал 6/10, Контент 8/10, Конверсия нового посетителя 6/10, Потенциал развития 10/10.

**Главная мысль отчёта:** не переделывать сайт в «очередной красивый магазин косметики» — сила сайта в том, что это сервис для профессионального мастера, а не витрина. Предложена рамка **4 ядра**: 🛒 Купить (каталог+прайс+заявка), 🎨 Подобрать (палитры+фильтры), 🧪 Рассчитать (формулы+пропорции), 📚 Узнать (база знаний+статьи) — всё связано между собой (пример сценария: ищешь оттенок → видишь товар → видишь формулу → видишь активатор → «добавить всё в заявку»).

**27 идей, сгруппированные по темам (не по одной, для читаемости — полный дословный текст отчёта не сохранён, только суть):**

1. *Навигация и два режима использования.* Три главные кнопки на входе (Каталог / Прайс Excel / Акции) вместо текущего «разбирайся сам». Разделить сценарии «я знаю что мне нужно» (быстрый поиск, напр. «Shinefinity 09/65» → сразу товар+цена+в заявку) и «я хочу посмотреть» (каталог по брендам → линейкам → карточки). Отдельно для новых посетителей — понятный путь «кто вы → что продаёте → почему у вас».
2. *Умный поиск как фишка сайта.* Развить нынешний поиск по прайсу до понимания запросов вида «wella 1.9» (все подходящие оксиды) или «холодный блонд» (подборка релевантных товаров+уходовых средств из разных линеек).
3. *«Помощник колориста» — подбор по задаче.* Раздел с вопросом «что вы хотите сделать» (окрашивание/тонирование/нейтрализация/осветление/седина/уход) → сайт предлагает продукты под задачу.
4. *База знаний как отдельный продукт.* Текущий блок «Инфо» развить в «Базу колориста» с категориями (окрашивание/осветление/уход/технологии), позиционировать не как довесок к магазину, а как отдельный актив бренда.
5. *Связка статья → товары → заказ.* В каждой гид-статье — не только теория, но и блок «использованные продукты» с ценами и кнопкой «добавить всё в заявку» прямо из статьи.
6. *Постоянно видимая «Моя заявка».* Не только модалка по клику — плавающий элемент вида «🛒 Моя заявка — 7 товаров», всегда на экране, с итоговой суммой. — частично сделано 2026-08-27: на мобильной плашке навигации есть пункт «Корзина» с бейджем-счётчиком, теперь верно работающим на любой странице сайта (не только главной) благодаря переходу на localStorage (см. раздел «Статус» выше). Это переход к корзине, не постоянно развёрнутая сумма прямо на экране — сама идея полностью ещё не закрыта, но техническая база (корзина, живущая вне одной страницы) теперь есть.
7. *Осознанно НЕ делать классический интернет-магазин.* Явная рекомендация не тащить оплату картой/полноценную корзину/Ozon-подобный UX — B2B-модель «набрал → заявка → подтверждение по контакту» подходит лучше. **Согласуется с тем, что сайт и так уже устроен** — не новая идея, а подтверждение текущего направления.
8. *Статусы наличия вместо текста в названии.* Убрать «не заказывать!» и подобное из названий товаров (см. проверенный факт выше), сделать отдельными статус-метками: 🟢 в наличии / 🟡 под заказ / 🔴 нет в наличии / 🔥 акция / 🆕 новинка.
9. *Фильтры по оттенкам.* Для линеек вроде Shinefinity — фильтры по уровню (4–10), направлению (Natural/Ash/Pearl/Beige/Gold/Copper/Violet), эффекту (нейтрализация/холодный/тёплый/пастельный).
10. *Визуальные палитры оттенков.* Не список текстом, а кликабельные квадраты-образцы с карточкой оттенка (фото/описание/цена/наличие/активатор/рекомендации) по клику. — 🟡 **пилот сделан 2026-08-27** на одной линейке (Illumina), подробности в разделе «Статус» выше — тиражировать на остальные линейки ещё не решено.
11. *«Формула → добавить всё».* Готовые рецепты (например «формула холодного блонда» — конкретные товары+граммовки) с одной кнопкой добавления всех компонентов в заявку.
12. *Усилить блок «Акции».* Подкатегории (скидка до 30%/распродажа/акция недели), обязательно показывать зачёркнутую старую цену рядом с новой, не просто новую цифру. — ✅ **сделано 2026-08-27**, подробности в разделе «Статус» выше (подкатегории по фактическим ступеням скидки 15%/7%, не по гипотетическим из идеи).
13. *«Новинки».* Отдельный фильтр/блок — что добавилось в прайс с последнего обновления. — ✅ **сделано 2026-08-27**, подробности в разделе «Статус» выше (в итоге — бейдж у товара, не отдельный блок/кнопка).
14. *«Избранное» / «Мой постоянный заказ» / «Повторить заказ».* Для мастеров, покупающих одно и то же регулярно — сохранённый набор товаров и кнопка «повторить», можно без полноценной авторизации (локальное хранилище браузера или именная ссылка).
15. *SEO-структура по URL.* Отдельные страницы-адреса вида `/wella/shinefinity/`, `/wella/shinefinity/09-65/`, `/blog/toning-after-lightening/` — каждая статья должна отвечать на конкретный рабочий вопрос мастера («как затонировать блонд после осветления»), это одновременно SEO и продажи.
16. *Telegram как полноценный канал сайта.* Явный блок-приглашение «ZILMA в Telegram» (цены/акции/новинки/формулы) со ссылкой с сайта.
17. *Уведомления о снижении цены.* Кнопка «следить за ценой» на товаре — уведомление, когда цена меняется (CRM-функция).
18. *Визуальная эстетика.* Минимализм: белый/глубокий красный/чёрный, чистая типографика — «профессиональная цифровая платформа», не «магазин косметики».
19. *Явное разделение двух аудиторий* (постоянный мастер vs новый посетитель) на уровне интерфейса — см. пункт 1.
20. *Отдельная страница «Для салонов».* B2B-лендинг внутри сайта: расходники, регулярные поставки, индивидуальные условия, форма заявки.
21. *Отдельная страница «Бренды».* Не только внутри прайса — красивая страница со списком брендов, переходом к линейкам каждого.
22. *Не прятать одноразовую продукцию/расходники.* Ассортимент реально широкий (одноразовая продукция, фольга, перчатки и т.п.) — показывать это как часть позиционирования «всё для салона», не стесняться.

**Приоритеты по этапам (как предложено в отчёте, ориентир, не обязательство):**
- 🔴 Этап 1 (по мнению автора отчёта — обязательно): перестроить навигацию, мощный поиск, отдельный каталог, страницы брендов, нормальные карточки товара, статусы наличия, постоянная «Моя заявка».
- 🟠 Этап 2: палитры, фильтры оттенков, формулы колориста, «добавить всё из формулы», избранное, повторный заказ.
- 🟡 Этап 3: база знаний, SEO-статьи, связка статья↔товары, подбор по задаче, помощник колориста.
- 🟢 Этап 4: Telegram-интеграция, уведомления о ценах, персональные предложения, история заказов, B2B-кабинет.

**Мой (Claude) комментарий, не для замены решения пользователя:** большая часть этапа 1 и 2 — реально крупные фичи (поиск с NLU-пониманием запросов, палитры-конструктор, формулы с автодобавлением, статусы наличия — это уже требует структурных изменений `priceItems.json`/схемы данных, не косметика). Прежде чем брать что-то в работу — решить масштаб и бюджет с пользователем, не начинать молча с «очевидных» первых пунктов. Некоторые вещи уже пересекаются с существующими пунктами плана выше (страница-каталог с SEO-урлами по бренду — уже есть в «Идеи пользователя»; статусы наличия — новое, стоит добавить туда же после того как пользователь определится, брать ли в работу).
